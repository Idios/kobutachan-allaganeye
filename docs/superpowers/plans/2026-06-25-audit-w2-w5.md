# W5 テスト配線拡充 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 「commit 済みだが pytest 未配線」のテスト資産 (split SHA baseline / VTuber ground truth) を配線し、export wire protocol e2e を CI 実走化し、test 系 P3 (collection 低速化 / ErrorModal a11y / flow mock 寛容 / README stale) を消化する。

**Architecture:** 既存 test ファイル群への追加・修正が主体。新規 production code は無し (test infra のみ)。Python (`tests/`) + GUI (`gui/src/`) + doc を touch する単一 PR。

**Tech Stack:** pytest (slow/slow_detect markers)、vitest + jest-axe、ffmpeg (lavfi/copy remux)、hashlib。

**Issue:** #844 / **spec:** docs/superpowers/specs/2026-06-10-audit-remediation-design.md §W5 / **base:** origin/develop-0.3.0

---

## 設計判断 (brainstorm 2026-06-25、Idios 承認済)

| 判断点 | 確定 |
| --- | --- |
| D1 wire e2e (P2-23) | **CI 実走化**: fixture を `-c:v mpeg4 -g 1` (LGPL-safe / 全 I-frame) に、export を `--codec copy` (default) に、`slow` marker 除去 + ffmpeg 不在 skip、docstring 修正 |
| D2 SIGINT test (P2-23) | **削除** (POSIX 専用 / 本番 GUI cancel は kill_tracked_processes / copy codec で interrupt window 消失) |
| D3 split 検証 (P2-22) | test は 5 baseline parametrize。**代理 detached 実行は obs-20260209 (+ obs-20260127)** で green 実証 |
| D4 VTuber env var (P2-24) | **既存 `ALLAGANEYE_SAMPLE_VIDEO_DIR_VTUBER` 再利用** (spec の新設案 `ALLAGANEYE_VTUBER_VIDEO_DIR` は stale) |
| D5 PR 構造 | **単一 PR** (test-infra 一貫テーマ) |
| P2-24 strictness | **`xfail(strict=False, reason=#809)`** (本番 VTuber 検出 #809 未 wiring のため、red にせず gate を配線) |

## File Structure

| ファイル | 操作 | 責務 (本 PR で何を変えるか) |
| --- | --- | --- |
| `tests/test_regression_330.py` | Modify | module-level `_gpu_available()` 呼出を lazy 化 (T1) |
| `tests/test_export_wire_protocol.py` | Modify | docstring 修正 / fixture を mpeg4 化 / `--codec copy` / slow 除去 + ffmpeg skip / SIGINT test 削除 (T2) |
| `tests/test_v030_baseline_regression.py` | Modify | split SHA bit-exact gate + 比較ヘルパ + ヘルパの fast unit test を追加 (T3) |
| `tests/test_vtuber_ground_truth.py` | Create | VTuber GT +-10s 照合 (xfail) を新規配線 (T4) |
| `gui/src/components/ErrorModal.test.tsx` | Modify | jest-axe a11y test 追加 (T5) |
| `gui/src/__tests__/flow.integration.test.tsx` | Modify | `configureHappyInvoke` の default を strict 化 + red 実証 test (T6) |
| `tests/baselines/v0.3.0/README.md` | Modify | stale な Rust 参照を `tests/test_export_ffmpeg_runner.py` に修正 (T7) |

## 横断規約 (全 task 共通)

- **ASCII guard**: `.py` の docstring/comment/code は ASCII のみ (`+-` / `->` を使い `±` `→` 禁止)。`test_ascii_guard` を含む full `python -m pytest` を production-code 変更 task ごとに回す。
- **commit trailer**: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- **branch attach**: task commit ごとに `git symbolic-ref -q --short HEAD` = `claude/audit-w2-w5` を検証 (detach 時は `git branch -f` + `git checkout`)。
- **検証ツール**: `python -m ruff` / `python -m pyright allaganeye tests` / `python -m pytest`。GUI は `cd gui && npm run lint/typecheck/test/build` + `cd gui/src-tauri && cargo check`。

---

### Task 1: regression_330 の GPU 判定を lazy 化 (AC-4)

**Files:**
- Modify: `tests/test_regression_330.py:48-67,132-138`

問題: `_gpu_available()` が module-level (`gpu_available = pytest.mark.skipif(not _gpu_available(), ...)`) で評価され、`pytest` collection (slow を deselect する default 実行でも module import は走る) ごとに `ffmpeg -hwaccels` subprocess を起動する。

- [ ] **Step 1: 現状の firing を観測 (red baseline)**

Run: `python -m pytest tests/test_regression_330.py --collect-only -q`
Expected: collection は成功するが、内部で `_gpu_available()` -> `ffmpeg -hwaccels` が起動している (module import 時)。`gpu_available` mark が module top-level で `_gpu_available()` を呼んでいることを `grep -n "_gpu_available()" tests/test_regression_330.py` で確認 (line 65 付近に module-level 呼出が在る)。

- [ ] **Step 2: module-level mark を削除し、GPU test 内 lazy skip に置換**

`gpu_available = pytest.mark.skipif(...)` ブロック (line 64-67) を削除する。GPU test (`test_match_6_boundary_2015_within_tolerance_gpu`) から `@gpu_available` decorator を外し、本体冒頭で lazy skip する:

```python
    @pytest.mark.slow_detect
    def test_match_6_boundary_2015_within_tolerance_gpu(
        self, primary_video: Path, primary_metadata: ProbeResult
    ) -> None:
        if not _gpu_available():
            pytest.skip("No GPU hardware acceleration available")
        boundaries = _run_detection(primary_video, primary_metadata, use_gpu=True)
        _assert_match_6_end_within_tolerance(boundaries)
```

`_gpu_available()` 関数定義 (line 48-61) はそのまま残す (呼び出しのみ test 内に移す)。

- [ ] **Step 3: collection が subprocess を起こさないことを検証 (green)**

`_gpu_available` を monkeypatch して collection 中に呼ばれないことを示す軽量 meta-test を追加 (file 末尾):

```python
def test_gpu_probe_is_not_called_at_import(monkeypatch: pytest.MonkeyPatch) -> None:
    """Regression: GPU availability probe must be lazy, not run at collection.

    The module-level skipif (removed) used to spawn ``ffmpeg -hwaccels`` on every
    pytest collection. This pins that the probe helper is only referenced from
    inside test bodies, not at import time.
    """
    import ast
    import pathlib

    src = pathlib.Path(__file__).read_text(encoding="utf-8")
    tree = ast.parse(src)
    # Top-level (module body) statements must not call _gpu_available().
    for node in tree.body:
        for sub in ast.walk(node):
            if (
                isinstance(sub, ast.Call)
                and isinstance(sub.func, ast.Name)
                and sub.func.id == "_gpu_available"
            ):
                # allowed only inside a FunctionDef/ClassDef, not module body
                raise AssertionError(
                    "_gpu_available() called at module level; keep it lazy"
                )
```

注: この meta-test は AST を見るだけで subprocess を起こさない (非 slow、CI で常時走る)。`ast.walk(node)` は各 top-level statement (FunctionDef 含む) を走査するため、FunctionDef 内部の呼出も拾ってしまう。これを避けるため、top-level の `FunctionDef`/`ClassDef`/`If`(skipif 相当) のみを対象にする版に補正する:

```python
def test_gpu_probe_is_not_called_at_import() -> None:
    """Regression: GPU availability probe must be lazy, not run at collection."""
    import ast
    import pathlib

    src = pathlib.Path(__file__).read_text(encoding="utf-8")
    tree = ast.parse(src)
    for node in tree.body:
        # Skip function/class bodies -- those run at call time, not import.
        if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef, ast.ClassDef)):
            continue
        for sub in ast.walk(node):
            if (
                isinstance(sub, ast.Call)
                and isinstance(sub.func, ast.Name)
                and sub.func.id == "_gpu_available"
            ):
                raise AssertionError(
                    "_gpu_available() called at module level; keep it lazy"
                )
```

- [ ] **Step 4: 実行して green 確認 + 全体 collection 健全性**

Run: `python -m pytest tests/test_regression_330.py::test_gpu_probe_is_not_called_at_import -v`
Expected: PASS

Run: `python -m pytest tests/test_regression_330.py --collect-only -q`
Expected: 6 items collected、エラーなし。

- [ ] **Step 5: full pytest + ascii-guard + commit**

Run: `python -m pytest -q` (full、slow は default deselect) -> green、`python -m ruff check tests/test_regression_330.py` -> clean。

```bash
git add tests/test_regression_330.py
git commit -m "test(w5): regression_330 の GPU 判定を lazy 化 (collection で subprocess 抑止) (Refs #844)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: wire e2e の CI 実走化 + SIGINT 削除 (AC-2)

**Files:**
- Modify: `tests/test_export_wire_protocol.py` (全面)

方針 (D1/D2): fixture を LGPL-safe encoder (`mpeg4 -g 1`) に、export を `--codec copy` に、`slow` marker を外し ffmpeg 不在時 skip、docstring を実態に修正、SIGINT test を削除。これで CI (LGPL ffmpeg、libx264 非同梱) の標準 `pytest` で GUI<->Python 契約 e2e が実走する。

- [ ] **Step 1: 現状を観測 (red/misleading baseline)**

`grep -n "slow\|libx264\|ALLAGANEYE_SAMPLE_VIDEO_DIR\|sigint\|SIGINT" tests/test_export_wire_protocol.py` で、(a) docstring の誤った skip claim (line 10)、(b) `pytestmark = pytest.mark.slow` (line 23)、(c) fixture の `-c:v libx264` (line 41)、(d) test の `--codec h264` (line 82-83)、(e) SIGINT test (line 119-176) を確認。

Run: `python -m pytest tests/test_export_wire_protocol.py --collect-only -q`
Expected: 2 tests collected だが両方 `slow` で default deselect (CI で走らない = 監査指摘の "唯一の e2e が常時 deselect")。

- [ ] **Step 2: docstring を実態に修正 + slow marker 除去 + ffmpeg-availability import**

ファイル冒頭の docstring を以下に置換 (ASCII、`->` 表記):

```python
"""End-to-end wire protocol test (#761, Codex review #5; CI-wired in #844 W5).

Spawns a real Python subprocess (``python -m allaganeye export --stdin --json``)
with a tiny ffmpeg-generated test video and ``--codec copy`` (no re-encode, so
no libx264 dependency) and asserts that:
  1. stdout is parseable ndjson
  2. event ordering is sane (progress* -> result | error -> summary terminal)
  3. each match_index produces a terminal result or error event
  4. summary line is the LAST line

Runs in CI (BtbN LGPL ffmpeg has no libx264, but copy/remux + mpeg4 fixture
need none). Skipped only when no ffmpeg binary is discoverable.
"""
```

`pytestmark = pytest.mark.slow` (line 23) を削除。import 部に `find_ffmpeg` を追加:

```python
from allaganeye.ffmpeg_path import find_ffmpeg
```

- [ ] **Step 3: fixture を LGPL-safe encoder + ffmpeg skip guard に**

`short_test_video` fixture を置換:

```python
@pytest.fixture(scope="module")
def short_test_video(tmp_path_factory: pytest.TempPathFactory) -> Path:
    """Generate a 6-second all-intra mpeg4 test video (LGPL-ffmpeg safe).

    mpeg4 + ``-g 1`` makes every frame a keyframe so ``-c copy`` can cut at any
    boundary. Skipped when no ffmpeg binary is available.
    """
    try:
        ffmpeg = find_ffmpeg()
    except Exception:
        pytest.skip("ffmpeg binary not available")
    out = tmp_path_factory.mktemp("export-wire") / "in.mp4"
    subprocess.run(
        [
            ffmpeg,
            "-y",
            "-f",
            "lavfi",
            "-i",
            "testsrc=duration=6:size=640x360:rate=30",
            "-pix_fmt",
            "yuv420p",
            "-c:v",
            "mpeg4",
            "-g",
            "1",
            str(out),
        ],
        check=True,
        capture_output=True,
    )
    return out
```

- [ ] **Step 4: e2e test を `--codec copy` に**

`test_wire_protocol_end_to_end` の export subprocess 引数 `--codec h264` を `--codec copy` に変更 (line 81-82 の `"--codec", "h264"` -> `"--codec", "copy"`)。他の assertion (summary success==2 / terminals_per_match=={0:1,1:1} / progress before terminal) はそのまま (protocol は codec 非依存)。`fallback` 許容の assertion (line 116) も copy では fallback が出ないだけで成立する。

- [ ] **Step 5: SIGINT test を削除**

`test_wire_protocol_cancel_via_sigint` (line 119-176、関数全体) を削除する。これで dead test (どの環境でも走らない) が解消される。

- [ ] **Step 6: 実行して CI 相当で green 確認**

Run: `python -m pytest tests/test_export_wire_protocol.py -v`
Expected: `test_wire_protocol_end_to_end` PASS (slow 無しで default 実行されること = collection で deselect されないことが要点)。ffmpeg がある環境では実走、無ければ skip。

> 補足: copy + mpeg4(-g 1) で 0-2s / 2-4s の cut が両方 result を返し `success==2` になることを実走で確認する。もし copy の境界精度で 1 match でも error 化する場合は、fixture の rate/GOP を調整 (例: `-g 1` 維持のまま `-r 30`) して全 keyframe を担保する。

- [ ] **Step 7: full pytest + lint + commit**

Run: `python -m pytest -q` -> green、`python -m ruff check tests/test_export_wire_protocol.py` + `python -m pyright tests/test_export_wire_protocol.py` -> clean。

```bash
git add tests/test_export_wire_protocol.py
git commit -m "test(w5): wire e2e を CI 実走化 (mpeg4 fixture + --codec copy) + SIGINT dead test 削除 (Refs #844)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: split SHA-256 bit-exact gate (AC-1)

**Files:**
- Modify: `tests/test_v030_baseline_regression.py` (追加: import hashlib / 比較ヘルパ / fast unit test / slow parametrized gate)

方針: detect Class A (`test_class_a_bit_exact`) と同型。比較ロジックを純関数ヘルパに切り出し、(a) 動画不要の fast unit test で red/green を担保、(b) slow_detect の parametrized gate で実 video 照合。

- [ ] **Step 1: 比較ヘルパの fast unit test を書く (red)**

ファイル先頭の import に `import hashlib` を追加。新規ヘルパ + fast unit test を追加 (file 末尾):

```python
def _sha256_file(path: Path) -> str:
    """Chunked SHA-256 (avoids loading multi-GB split outputs into memory)."""
    h = hashlib.sha256()
    with path.open("rb") as fh:
        for chunk in iter(lambda: fh.read(1024 * 1024), b""):
            h.update(chunk)
    return h.hexdigest()


def _diff_split_against_baseline(out_dir: Path, expected_splits: list[dict]) -> list[str]:
    """Return human-readable mismatch lines; empty list == bit-exact match."""
    problems: list[str] = []
    for sp in expected_splits:
        produced = out_dir / sp["output_file"]
        if not produced.exists():
            problems.append(f"missing output: {sp['output_file']}")
            continue
        size = produced.stat().st_size
        if size != sp["size_bytes"]:
            problems.append(
                f"{sp['output_file']} size {size} != {sp['size_bytes']}"
            )
        sha = _sha256_file(produced)
        if sha != sp["sha256"]:
            problems.append(
                f"{sp['output_file']} sha256 {sha[:12]} != {sp['sha256'][:12]}"
            )
    return problems


@pytest.mark.parametrize("baseline_sha", ["aa" * 32])
def test_diff_split_helper_detects_mismatch(baseline_sha, tmp_path):
    """The split comparator must FIRE on a sha/size mismatch (red verification)."""
    f = tmp_path / "match_001.mp4"
    f.write_bytes(b"hello world")
    real_sha = _sha256_file(f)
    # matching baseline -> no problems
    ok = _diff_split_against_baseline(
        tmp_path,
        [{"output_file": "match_001.mp4", "size_bytes": 11, "sha256": real_sha}],
    )
    assert ok == []
    # wrong sha + wrong size + missing file -> 3 problems
    bad = _diff_split_against_baseline(
        tmp_path,
        [
            {"output_file": "match_001.mp4", "size_bytes": 999, "sha256": baseline_sha},
            {"output_file": "match_002.mp4", "size_bytes": 1, "sha256": baseline_sha},
        ],
    )
    assert any("size" in p for p in bad)
    assert any("sha256" in p for p in bad)
    assert any("missing" in p for p in bad)
```

Run: `python -m pytest "tests/test_v030_baseline_regression.py::test_diff_split_helper_detects_mismatch" -v`

> 注: この test 自体は `slow`/`slow_detect` の module pytestmark を継承するため default では deselect される。fast 検証のため `-m slow` を付けて単体実行する。`module pytestmark` を override したくないので、本 helper test は意図的に slow 扱いのまま (実 video 不要だが marker 継承)。red/green 確認は `-m slow` で行う。

Expected: PASS (helper が mismatch を検出)。先に helper 未定義状態で走らせれば import error = red。

- [ ] **Step 2: slow_detect の bit-exact gate を追加**

`_CLASS_A_BASELINES` の下に split baseline 一覧 + gate を追加:

```python
# Split bit-exact baselines (#779 generated; P2-22 / #844 wires the gate).
_SPLIT_BASELINES = [
    ("obs-20260116", "20260116/2026-01-16 22-12-57.mkv"),
    ("obs-20260118", "20260118/2026-01-18 22-15-18.mkv"),
    ("obs-20260119", "20260119/2026-01-19 22-09-07.mkv"),
    ("obs-20260127", "20260127/2026-01-27 21-59-15.mkv"),
    ("obs-20260209", "2026-02-09 23-12-24.mkv"),
]


@pytest.mark.parametrize("label,relpath", _SPLIT_BASELINES)
def test_split_bit_exact(label, relpath, sample_video_dir, tmp_output_dir):
    """split --from-metadata output SHA-256 + size matches obs-*.split.json (P2-22).

    detect Class A bit-exact analogue for the splitter (-c copy remux). Rewrites
    the baseline metadata 'source' to the local video, runs split, and byte-checks
    each produced match file against the committed split baseline.
    """
    video = sample_video_dir / relpath
    if not video.exists():
        pytest.skip(f"video not found: {video}")
    split_baseline = _BASELINE_DIR / f"{label}.split.json"
    meta_baseline = _BASELINE_DIR / f"{label}.metadata.json"
    if not split_baseline.exists() or not meta_baseline.exists():
        pytest.skip(f"baseline missing for {label}")

    meta = json.loads(meta_baseline.read_text(encoding="utf-8"))
    meta["source"] = str(video.resolve())
    in_meta = tmp_output_dir / "metadata.json"
    in_meta.write_text(json.dumps(meta), encoding="utf-8")
    out_dir = tmp_output_dir / "splits"
    out_dir.mkdir()

    cmd = [
        sys.executable,
        "-m",
        "allaganeye",
        "split",
        "--from-metadata",
        str(in_meta),
        "-o",
        str(out_dir),
    ]
    result = subprocess.run(cmd, capture_output=True, text=True, timeout=3600)
    assert result.returncode == 0, f"split failed: {result.stderr}"

    expected = json.loads(split_baseline.read_text(encoding="utf-8"))
    problems = _diff_split_against_baseline(out_dir, expected["splits"])
    assert not problems, f"split bit-exact diff for {label}:\n" + "\n".join(problems)
```

- [ ] **Step 3: collection 健全性**

Run: `python -m pytest tests/test_v030_baseline_regression.py --collect-only -q -m slow_detect`
Expected: 既存 + `test_split_bit_exact[obs-2026....]` 5 件が collected。エラーなし。

- [ ] **Step 4: full pytest + lint + commit**

Run: `python -m pytest -q` -> green (slow は deselect なので split gate は走らず helper test も deselect)。`python -m ruff check tests/test_v030_baseline_regression.py` + `python -m pyright tests/test_v030_baseline_regression.py` -> clean。

```bash
git add tests/test_v030_baseline_regression.py
git commit -m "test(w5): split --from-metadata の SHA-256 bit-exact gate を追加 (P2-22) (Refs #844)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

> 代理検証 (Pre-flight): obs-20260209 (+ obs-20260127) で `ALLAGANEYE_SAMPLE_VIDEO_DIR=E:\royalstraightflesh\videos` + `python -m pytest "tests/test_v030_baseline_regression.py::test_split_bit_exact" -m slow_detect -k "20260209 or 20260127"` を detached 実行し green を確認。

---

### Task 4: VTuber ground truth +-10s gate (AC-3)

**Files:**
- Create: `tests/test_vtuber_ground_truth.py`

方針: 既存 env var `ALLAGANEYE_SAMPLE_VIDEO_DIR_VTUBER` で VOD を解決し、`allaganeye detect --vtuber` の matches を GT (`vtuber-primary-ground-truth.json`) と +-10s 照合。本番 VTuber 検出 (#809) 未 wiring のため `xfail(strict=False)`。

- [ ] **Step 1: 新規ファイルを作成**

```python
"""VTuber ground truth +-10s acceptance gate (P2-24, #809 prerequisite; #844 W5).

slow + slow_detect: real VTuber VOD required, CI deselect.
VOD dir: ALLAGANEYE_SAMPLE_VIDEO_DIR_VTUBER (default E:/allaganeye-samples),
reusing the env var established by tests/test_vtuber_region_e2e.py.

Marked xfail(strict=False) until #809 wires production VTuber detection: the
gate documents the +-10s target and becomes a hard pass-gate (drop xfail) once
calibration lands. guard verify FP-rejects large VODs -> owner override, PYTHONUTF8=1.
"""

from __future__ import annotations

import json
import os
import subprocess
import sys
from pathlib import Path

import pytest

pytestmark = [pytest.mark.slow, pytest.mark.slow_detect]

_REPO_ROOT = Path(__file__).resolve().parent.parent
_GT_PATH = _REPO_ROOT / "tests" / "baselines" / "v0.3.0" / "vtuber-primary-ground-truth.json"
_VTUBER_DIR = Path(
    os.environ.get("ALLAGANEYE_SAMPLE_VIDEO_DIR_VTUBER") or r"E:/allaganeye-samples"
)


def _ground_truth() -> dict:
    return json.loads(_GT_PATH.read_text(encoding="utf-8"))


def _resolve_vod() -> Path | None:
    """Find the GT source VOD under the VTuber dir (top-level or one subdir)."""
    if not _VTUBER_DIR.exists():
        return None
    name = _ground_truth()["source_file"]
    direct = _VTUBER_DIR / name
    if direct.is_file():
        return direct
    for match in _VTUBER_DIR.glob(f"**/{name}"):
        if match.is_file():
            return match
    return None


@pytest.mark.xfail(
    reason="#809 production VTuber detection calibration pending; gate wired ahead",
    strict=False,
)
def test_vtuber_ground_truth_within_tolerance(tmp_output_dir):
    """Each GT FL match has a detected match with start+end within +-tolerance_sec."""
    vod = _resolve_vod()
    if vod is None:
        pytest.skip(
            "VTuber GT VOD not found under ALLAGANEYE_SAMPLE_VIDEO_DIR_VTUBER"
        )
    gt = _ground_truth()
    tol = float(gt["tolerance_sec"])

    cmd = [
        sys.executable,
        "-m",
        "allaganeye",
        "detect",
        str(vod),
        "--vtuber",
        "-o",
        str(tmp_output_dir),
        "--no-cache",
    ]
    result = subprocess.run(cmd, capture_output=True, text=True, timeout=3600)
    assert result.returncode == 0, f"detect failed: {result.stderr}"
    detected = json.loads(
        (tmp_output_dir / "metadata.json").read_text(encoding="utf-8")
    )["matches"]

    misses: list[str] = []
    for g in gt["matches"]:
        near = [
            d
            for d in detected
            if abs(float(d["start_time"]) - float(g["start_time"])) <= tol
            and abs(float(d["end_time"]) - float(g["end_time"])) <= tol
        ]
        if not near:
            misses.append(
                f"GT match {g['index']} ({g['start_time']}-{g['end_time']}s) "
                f"has no detected match within +-{tol:.0f}s"
            )
    assert not misses, "VTuber GT +-tol mismatch:\n" + "\n".join(misses)
```

注: `tmp_output_dir` fixture は conftest.py (line 113) 提供。`--vtuber` flag は cli.py:152 で存在確認済み。

- [ ] **Step 2: collection 健全性 + xfail 配線の確認**

Run: `python -m pytest tests/test_vtuber_ground_truth.py --collect-only -q -m slow_detect`
Expected: 1 item collected (`test_vtuber_ground_truth_within_tolerance`)、エラーなし。

Run (VOD 不在環境): `python -m pytest tests/test_vtuber_ground_truth.py -m slow_detect -v`
Expected: SKIPPED ("VTuber GT VOD not found ...") -- VOD 不在では skip、red にならない。

- [ ] **Step 3: full pytest + lint + commit**

Run: `python -m pytest -q` -> green、`python -m ruff check tests/test_vtuber_ground_truth.py` + `python -m pyright tests/test_vtuber_ground_truth.py` -> clean。

```bash
git add tests/test_vtuber_ground_truth.py
git commit -m "test(w5): VTuber ground truth +-10s gate を pytest 化 (xfail, #809 待ち) (Refs #844)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

> 代理検証 (best-effort、Pre-flight): VOD 在環境で本 test を detached 実行し、xfail/xpass と実 +-delta を PR 本文に記録 (gate なので red 化はしない)。VOD 入手は memory reference_vtuber_multisource_samples (E:\allaganeye-samples)。

---

### Task 5: ErrorModal jest-axe a11y test (AC-5)

**Files:**
- Modify: `gui/src/components/ErrorModal.test.tsx`

`toHaveNoViolations` は test-setup.ts (line 9) で global extend 済み。`axe` を import して a11y test を追加 (ConfirmExitModal.test.tsx と同パターン)。

- [ ] **Step 1: import + a11y test を追加**

ファイル先頭 import に追加:

```tsx
import { axe } from 'jest-axe';
```

最初の `describe('ErrorModal', ...)` 内 (末尾の `'has role=dialog and aria-modal'` test の後) に追加:

```tsx
  it('has no axe violations (recoverable js-error)', async () => {
    useErrorStore.getState().showError({
      errorMessage: 'something failed',
      errorStack: 'at line 1',
      errorCategory: 'js-error',
      isPanic: false,
      isRecoverable: true,
    });
    const { container } = render(<ErrorModal />);
    expect(await axe(container)).toHaveNoViolations();
  });

  it('has no axe violations (panic, non-recoverable)', async () => {
    useErrorStore.getState().showError({
      errorMessage: 'panic msg',
      errorCategory: 'panic',
      isPanic: true,
      isRecoverable: false,
    });
    const { container } = render(<ErrorModal />);
    expect(await axe(container)).toHaveNoViolations();
  });
```

- [ ] **Step 2: 実行 (green、または violation なら ErrorModal.tsx を修正)**

Run: `cd gui && npm test -- ErrorModal`
Expected: PASS。**もし axe violation が出たら** (ErrorModal の a11y 不備が顕在化したら)、それは AC-5 の real finding なので `gui/src/components/ErrorModal.tsx` を最小修正して green にする (TDD red->green for a11y)。修正内容は PR 本文に記載。

- [ ] **Step 3: GUI gate + commit**

Run: `cd gui && npm run lint && npm run typecheck && npm test`
Expected: 全 PASS。

```bash
git add gui/src/components/ErrorModal.test.tsx gui/src/components/ErrorModal.tsx
git commit -m "test(w5): ErrorModal に jest-axe a11y 検査を追加 (全 modal 網羅) (Refs #844)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

> 注: ErrorModal.tsx を触らなかった場合は `git add` から外す。

---

### Task 6: flow.integration の invoke mock を strict 化 (AC-6)

**Files:**
- Modify: `gui/src/__tests__/flow.integration.test.tsx:163-165` (configureHappyInvoke の default) + red 実証 test 追加

問題 (audit line 148): `configureHappyInvoke()` の `default: return Promise.resolve()` により、app が新 command を invoke しても silent pass する。strict 化して未 mock command で reject させ、契約逸脱を検知させる。

- [ ] **Step 1: default を strict reject に変更**

`configureHappyInvoke` の `default:` case (line 163-164) を置換:

```tsx
      default:
        return Promise.reject(
          new Error(`unmocked invoke command: ${cmd}`),
        );
```

- [ ] **Step 2: red 実証 test を追加 (firing side)**

`configureHappyInvoke` 定義の直後 (または最初の describe 群の中) に regression guard を追加:

```tsx
describe('invoke mock strictness (#844 regression guard)', () => {
  it('configureHappyInvoke rejects unmocked commands (no silent pass)', async () => {
    configureHappyInvoke();
    await expect(
      invokeMock('some_command_that_does_not_exist'),
    ).rejects.toThrow(/unmocked invoke command/);
  });
});
```

- [ ] **Step 3: 実行 -- 既存 flow test が uncased command を炙り出すか確認**

Run: `cd gui && npm test -- flow.integration`
Expected: 新 guard test PASS。**既存 flow test が失敗したら**、それは app flow が `configureHappyInvoke` の switch に無い command を invoke している証拠 (まさに silent pass していた死角)。失敗 command を switch に happy-path 値で追加して green にする (各追加を commit message / PR に列挙)。全 green なら switch は既に網羅済み。

- [ ] **Step 4: GUI gate + commit**

Run: `cd gui && npm run lint && npm run typecheck && npm test`
Expected: 全 PASS。

```bash
git add gui/src/__tests__/flow.integration.test.tsx
git commit -m "test(w5): flow integration の invoke mock default を strict 化 (未 mock command で fail) (Refs #844)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: baselines README の stale Rust 参照を修正 (AC-7)

**Files:**
- Modify: `tests/baselines/v0.3.0/README.md:21`

`gui/src-tauri` に `select_h264_encoder`/`is_gpu_encoder_failure` は不在 (grep 確認済)。正は Python の `tests/test_export_ffmpeg_runner.py`。

- [ ] **Step 1: 該当セルを修正**

line 21 の "別の検証ルート" セル:

```text
| GUI H264 再エンコード (#591) | 入力 codec / GPU vendor / driver で出力 byte が変動 = 元々 deterministic regression に向かない | Tauri Rust 側の `select_h264_encoder` / `is_gpu_encoder_failure` unit test (`gui/src-tauri/`) |
```

を以下に置換:

```text
| GUI H264 再エンコード (#591) | 入力 codec / GPU vendor / driver で出力 byte が変動 = 元々 deterministic regression に向かない | Python 側の `is_gpu_encoder_failure` / encoder fallback unit test (`tests/test_export_ffmpeg_runner.py`、#761 で encoder ロジックを Python subprocess 化) |
```

- [ ] **Step 2: markdownlint + 参照先実在確認 + commit**

Run: `bash scripts/check-markdownlint.sh` (該当 .md が clean)。`ls tests/test_export_ffmpeg_runner.py` で参照先実在確認。

```bash
git add tests/baselines/v0.3.0/README.md
git commit -m "docs(w5): split baseline README の stale な Rust 参照を Python test に修正 (Refs #844)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Pre-flight (Iron Law 6、PR 作成前)

- [ ] Step 0: `gh pr list --search "844" --state open` ハードゲート (0 件確認)
- [ ] Step 1-2: `git fetch origin develop-0.3.0` + `git log HEAD..origin/develop-0.3.0` (取り込み未済確認)
- [ ] Step 3-4: touched files 交差 + 並行 PR 重複再確認 (`--state all`)
- [ ] Step 5: `/codex:adversarial-review` (companion script、`--wait --base origin/develop-0.3.0 --scope branch`、focus は ASCII: テスト wiring の false-green / skip-guard 漏れ / copy-codec event 構造 / xfail 誤用)
- [ ] 全 Python checks: `python -m ruff check .` (touched root) / `python -m ruff format --check .` / `python -m pyright allaganeye tests` / `python -m pytest -q`
- [ ] 全 GUI checks: `cd gui && npm run lint && npm run typecheck && npm test && npm run build` + `cd gui/src-tauri && cargo check`
- [ ] ascii-guard: `python -m pytest -q -k ascii_guard` green
- [ ] 代理検証 (detached、Start-Process Hidden): split bit-exact (obs-20260209 (+20260127)) green / VTuber GT (VOD 在環境) は xfail/xpass + delta 記録 (best-effort)

## 検証方針サマリ

| AC | CI で走る | 代理/実機検証 |
| --- | --- | --- |
| AC-1 split gate | helper unit (slow だが video 不要、`-m slow` で実証) | split bit-exact は obs-20260209(+27) detached |
| AC-2 wire e2e | **Yes** (slow 除去、CI 標準 pytest で実走) | -- |
| AC-3 VTuber GT | No (VOD 不在で skip) | xfail/xpass + delta (VOD 在環境、best-effort) |
| AC-4 regression_330 | Yes (meta-test 非 slow) | -- |
| AC-5 ErrorModal axe | Yes (vitest) | -- |
| AC-6 flow mock | Yes (vitest) | -- |
| AC-7 README | markdownlint | -- |

## Self-Review (記入)

- **Spec coverage**: §W5 骨子 3 本 (P2-22 split=T3 / P2-23 wire+SIGINT=T2 / P2-24 VTuber=T4) + test P3 4 本 (regression_330=T1 / ErrorModal axe=T5 / flow mock=T6 / README=T7) を全 task でカバー。
- **Placeholder scan**: 各 step に完全 code を記載。"violation なら修正" (T5)・"uncased command なら追加" (T6) は条件分岐の実手順として明示 (placeholder ではない)。
- **Type consistency**: `_diff_split_against_baseline` / `_sha256_file` (T3) は helper test と gate で同一 signature。`_resolve_vod` / `_ground_truth` (T4) は test 内で一貫。
- **ASCII**: 全 `.py` code block は `+-` / `->` 表記で ASCII 準拠。
