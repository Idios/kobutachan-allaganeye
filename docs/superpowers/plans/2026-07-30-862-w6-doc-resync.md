# W6: doc 一括再同期 (SSoT 適用) 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 2026-06-10 full audit の doc drift 項目 (P2-25〜P2-35 + doc 系 P3) を実装に再同期し、複製された workers 上限値を SSoT 参照へ寄せる。

**Architecture:** 変更は doc と Python docstring のみでロジック変更はゼロ。PR-A (CLI / コマンド仕様系) が `_resolve_workers` docstring を workers 上限の正として確立し、PR-B (アーキテクチャ説明系) がそれを参照する。両 PR は同じファイルを触らないため構造的にコンフリクトしない。

**Tech Stack:** Markdown (markdownlint-cli2) / Python docstring (ruff / pyright / pytest)

## Global Constraints

- spec: [`docs/superpowers/specs/2026-07-30-862-w6-doc-resync-design.md`](../specs/2026-07-30-862-w6-doc-resync-design.md)
- issue: [#862](https://github.com/Idios/kobutachan-allaganeye/issues/862)
- base ブランチ: `develop-0.3.0`。PR-A / PR-B とも `develop-0.3.0` を base にする
- 順序: **PR-A → PR-B**。PR-B の workers リンクが PR-A で確立した正を指すため
- commit / PR 本文で `Closes` / `Fixes` / `Resolves` を使わない。`Refs #862` のみ
- commit の trailer は `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
- 実測記録 7 箇所 (`benchmarks.md:28,36,50,58` / `video-processing.md:380,381` / `cli-spec.md:82`) の**数値を書き換えない**。書き換えは計測条件の改竄にあたる
- `docs/audits/` は 2026-06-10 時点の観測記録として書き換えない
- `docs/ui-interaction-spec.md` は触らない (別 issue、Task 19)
- 各 commit 前に `bash scripts/check-markdownlint.sh --fix` を実行する (`docs/superpowers/` も lint 対象)
- PR 作成前ゲートは**全 repo で引数なし実行**する: `python -m ruff check .` / `python -m ruff format --check .` / `python -m pyright` / `pytest`
- ローカル ruff は 0.15.11、CI は 0.16.0。CI 赤 + ローカル green のときは CI の版を確認して再現してから直す
- PR merge は Idios 専任。agent は merge せず `AskUserQuestion` で依頼する

## File Structure

### PR-A: CLI / コマンド仕様系

| ファイル | 責務 |
| --- | --- |
| `allaganeye/video/detector.py` | `_resolve_workers` docstring = workers 上限の**正** |
| `allaganeye/commands/split_matches.py` | `_workers_summary_str` docstring の誤値 24 を正へ |
| `tests/test_split_matches.py` | 同 test docstring の誤値 24 を正へ |
| `docs/cli-spec.md` | workers 仕様主張 3 箇所 / detect・split option 表 / export `--concurrency` / metadata トップレベル表 / `_load_cache_hit` 改名追従 |
| `docs/tuning-guide.md` | workers 仕様主張 2 箇所 / GPU default |
| `docs/benchmarks.md` | §CPU コア数 の workers 仕様主張のみ |
| `docs/output-spec.md` | 適用範囲に `detect` 追加 |
| `docs/developer-setup.md` | ffmpeg インストール手順を BtbN LGPL 優先へ |
| `docs/coding-conventions.md` | §背景 の引用値訂正 |

### PR-B: アーキテクチャ説明系

| ファイル | 責務 |
| --- | --- |
| `CLAUDE.md` | モジュール表 / §コマンド / L2 状態 / データフロー / #860 |
| `docs/design-overview.md` | L2 状態 |
| `docs/video-processing.md` | Pass 1 方式 / #576 新 path / workers リンク化 |
| `docs/scorebar-detection-design.md` | #803 中央跨ぎ + 幅上限 / #797 / #811 |
| `docs/system-architecture.md` | §2.1 起動経路 / §2.3 export 経路 |
| `docs/quickstart.md` | ZIP レイアウト |
| `docs/versioning.md` | version 3 箇所 / 旧用語 |
| `docs/metadata-spec.md` | #879 write 境界検証 |
| `docs/gui-development.md` | CI ジョブ表 |

---

## PR-A: CLI / コマンド仕様系

### Task 1: workers 上限の SSoT 正を確立する

**Files:**

- Modify: `allaganeye/video/detector.py:279-283`

**Interfaces:**

- Produces: `_resolve_workers` の docstring が「workers 上限 = `min(cpu_count, 32)`」の唯一の正。以降の doc はこの関数名を参照するだけで具体値を書かない

- [ ] **Step 1: docstring を正として書き直す**

`allaganeye/video/detector.py:279-283` の現状:

```python
def _resolve_workers(workers: int | None) -> int:
    """Resolve worker count: explicit value or auto-detect."""
    if workers is not None:
        return workers
    return min(os.cpu_count() or 4, 32)
```

これを次に置き換える (ロジックは 1 文字も変えない):

```python
def _resolve_workers(workers: int | None) -> int:
    """Resolve worker count: explicit value or auto-detect.

    **This docstring is the single source of truth for the worker cap.**
    Per ``docs/coding-conventions.md`` (doc SSoT convention, #818), spec
    docs must not duplicate the number -- they write "auto" and link here.

    When ``workers is None`` (CLI ``--workers`` omitted) the count is
    ``min(cpu_count, 32)``.  The cap exists because each worker spawns one
    ``ffmpeg -threads 1`` probe process; beyond ~32 concurrent seeks the
    bottleneck moves from CPU to storage I/O and throughput stops scaling.
    ``os.cpu_count()`` returning ``None`` falls back to 4.
    """
    if workers is not None:
        return workers
    return min(os.cpu_count() or 4, 32)
```

- [ ] **Step 2: ゲートを回す**

Run:

```bash
python -m ruff check .
python -m ruff format --check .
python -m pyright
```

Expected: いずれも失敗なし (docstring のみの変更)

- [ ] **Step 3: Commit**

```bash
git add allaganeye/video/detector.py
git commit -m "docs(#862): _resolve_workers docstring を workers 上限の SSoT 正にする (Refs #862)"
```

---

### Task 2: 誤値 24 が残る Python docstring 2 箇所を正へ寄せる

**Files:**

- Modify: `allaganeye/commands/split_matches.py:745-752`
- Modify: `tests/test_split_matches.py:5296-5301`

**Interfaces:**

- Consumes: Task 1 で確立した `_resolve_workers` docstring

- [ ] **Step 1: `_workers_summary_str` の docstring を直す**

`allaganeye/commands/split_matches.py:745-752` の現状:

```python
def _workers_summary_str(workers: int | None) -> str:
    """Format workers count for verbose summary, resolving ``auto`` (#389).

    When ``config.workers is None`` the CLI delegates to
    ``_resolve_workers`` which picks ``min(cpu_count, 24)``.  Users with
    performance issues need to see the *resolved* number to diagnose
    under-parallelised runs, not just the ``auto`` placeholder.
    """
```

これを次に置き換える:

```python
def _workers_summary_str(workers: int | None) -> str:
    """Format workers count for verbose summary, resolving ``auto`` (#389).

    When ``config.workers is None`` the CLI delegates to
    ``_resolve_workers``, whose docstring is the source of truth for the
    cap (#862).  Users with performance issues need to see the *resolved*
    number to diagnose under-parallelised runs, not just the ``auto``
    placeholder.
    """
```

- [ ] **Step 2: test の docstring を直す**

`tests/test_split_matches.py:5296-5301` の現状:

```python
    """Verbose summary resolves workers=auto to the actual count (#389).

    ``workers=auto`` alone doesn't let users diagnose CPU under-utilisation
    or hit the right number for their box; the resolved ``min(cpu_count,
    24)`` must be shown alongside.
    """
```

これを次に置き換える:

```python
    """Verbose summary resolves workers=auto to the actual count (#389).

    ``workers=auto`` alone doesn't let users diagnose CPU under-utilisation
    or hit the right number for their box; the number resolved by
    ``_resolve_workers`` must be shown alongside.
    """
```

- [ ] **Step 3: 残存 24 がないことを確認する**

Run:

```bash
grep -rn "cpu_count, 24\|min(cpu_count," allaganeye/ tests/
```

Expected: 出力なし (Task 1 の `min(os.cpu_count() or 4, 32)` は式が異なるため一致しない)

- [ ] **Step 4: ゲートを回す**

Run:

```bash
python -m ruff check .
python -m ruff format --check .
python -m pyright
pytest
```

Expected: 全 pass。docstring のみの変更のため既存 test の assertion には影響しない

- [ ] **Step 5: Commit**

```bash
git add allaganeye/commands/split_matches.py tests/test_split_matches.py
git commit -m "docs(#862): docstring に残った workers 上限 24 を SSoT 参照へ寄せる (Refs #862)"
```

---

### Task 3: `docs/cli-spec.md` の workers 仕様主張 3 箇所を SSoT 化する

**Files:**

- Modify: `docs/cli-spec.md:53` / `:269` / `:515`

**Interfaces:**

- Consumes: Task 1 の `_resolve_workers` docstring

**注意:** `docs/cli-spec.md:82` の `workers=auto (24)` は `allaganeye 0.1.1` と version 印字された実出力サンプルであり、**書き換えない**。

- [ ] **Step 1: split option 表 (`:53`) を直す**

現状:

```markdown
| `--workers` | auto | 検知の並列ワーカー数（デフォルト: 自動=`min(cpu_count, 24)`） |
```

置換後:

```markdown
| `--workers` | auto | 検知の並列ワーカー数。auto の解決値は実装の cap に従う (正: `allaganeye/video/detector.py` の `_resolve_workers` docstring) |
```

- [ ] **Step 2: `detection_params` 表 (`:269`) を直す**

現状:

```markdown
| `workers` | int \| null | 並列ワーカー数の指定値。`null` は auto（`_resolve_workers` が `min(cpu_count, 24)` で解決）を示す |
```

置換後:

```markdown
| `workers` | int \| null | 並列ワーカー数の指定値。`null` は auto (`_resolve_workers` が実装の cap で解決) を示す |
```

- [ ] **Step 3: debug-brightness option 表 (`:515`) を直す**

現状:

```markdown
| `--workers` | auto | 並列ワーカー数（デフォルト: 自動=`min(cpu_count, 24)`） |
```

置換後:

```markdown
| `--workers` | auto | 並列ワーカー数。auto の解決値は実装の cap に従う (正: `_resolve_workers` docstring) |
```

- [ ] **Step 4: `:82` が無傷であることを確認する**

Run:

```bash
grep -n "24" docs/cli-spec.md
```

Expected: `workers=auto (24)` を含む `:82` の 1 行のみが残る (他の 24 は消えている)

- [ ] **Step 5: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 6: Commit**

```bash
git add docs/cli-spec.md
git commit -m "docs(#862): cli-spec の workers 上限 3 箇所を SSoT 参照化 (Refs #862)"
```

---

### Task 4: `docs/cli-spec.md` の option 表を実装に再同期する (P2-34)

**Files:**

- Modify: `docs/cli-spec.md` split option 表 (`### オプション`、`--dry-run` 行の直前) / detect option 表 / export `--concurrency` 行 / `:146` (`_load_cache` 改名追従)

**注意:** `--vtuber` は `typer.Option(..., hidden=True)` で `--help` に出ない実験フラグのため、ユーザー向け option 表には**追加しない**。

- [ ] **Step 1: split option 表に `--masked` を追加する**

`docs/cli-spec.md` の split `### オプション` 表で、次の行の**直後**に追加する:

```markdown
| `--no-audio` | `false` | 音声ベースの試合境界昇格（Fanfare スキャン）を無効化する。**現在は音声モジュールが凍結中（#327）のため、本フラグの値に関わらずスキャンは常にスキップされる。verbose 出力では `audio=frozen` と表示される (#384)** |
```

追加する行:

```markdown
| `--masked` | `false` | チャット欄マスク画像が全画面に合成された録画向け。mask のない領域を自動検出して再検知する。暗転が一部見つかる場合でも本フラグ指定でこの経路を強制する。**`--vtuber` と同時指定は排他エラー (exit 5)** |
```

- [ ] **Step 2: detect option 表に `--masked` / `--gpu-vendor` / `--progress-format` を追加する**

detect の `### オプション` 表の現状 (該当部分):

```markdown
| `--workers` | auto | 検知の並列ワーカー数 |
| `--gpu` / `--no-gpu` | auto | GPU 強制 / CPU 強制 (排他) |
| `--no-cache` | `false` | キャッシュ無視で再検知 |
| `--keep-trailing` | `false` | post-match trailing の flagging を skip し通常 match として保持 (#805 段階2、default は flag して MP4 除外・metadata 保持) |
| `--no-audio` | `false` | 音声昇格無効化 (現在 frozen) |
| `-v`, `--verbose` | `false` | 詳細出力 |
| `-q`, `--quiet` | `false` | 進捗出力抑制 |
```

置換後:

```markdown
| `--workers` | auto | 検知の並列ワーカー数 |
| `--gpu` / `--no-gpu` | auto | GPU 強制 / CPU 強制 (排他) |
| `--gpu-vendor` | `auto` | 使用する GPU vendor を明示指定 (`auto` / `nvidia` / `amd` / `intel`)。split と同仕様 |
| `--no-cache` | `false` | キャッシュ無視で再検知 |
| `--keep-trailing` | `false` | post-match trailing の flagging を skip し通常 match として保持 (#805 段階2、default は flag して MP4 除外・metadata 保持) |
| `--no-audio` | `false` | 音声昇格無効化 (現在 frozen) |
| `--masked` | `false` | チャット欄マスク録画向けの mask-free 領域自動検出 + 再検知。split と同仕様 |
| `-v`, `--verbose` | `false` | 詳細出力 |
| `-q`, `--quiet` | `false` | 進捗出力抑制 |
| `--progress-format` | `text` | 進捗の出力形式。`text` は click progress bar + typer ステータス行、`json` は stdout に 1 行 1 JSON (`phase` / `completed` / `total` / `elapsed_s`) を emit し人間可読出力を全抑制する (Tauri GUI wrapper 用、#569)。`text` / `json` 以外は exit 5 |
```

- [ ] **Step 3: export `--concurrency` 行に codec=copy 規則を追記する**

現状:

```markdown
| `--concurrency N` | SKU テーブル値 | 同時 export スロット数を上書き (`enumerate_h264_encoders` が返す値のデフォルト: RTX 5090 → 3、RTX 4090/4080/4070 → 2、RTX 4060 / 不明 NVIDIA → 1、QSV / AMF / libx264 → 1) |
```

置換後:

```markdown
| `--concurrency N` | SKU テーブル値 | 同時 export スロット数を上書き (`enumerate_h264_encoders` が返す値のデフォルト: RTX 5090 → 3、RTX 4090/4080/4070 → 2、RTX 4060 / 不明 NVIDIA → 1、QSV / AMF / libx264 → 1)。**`--codec copy` 時は本フラグより先にスロットが 1 に切り詰められるため無効** — 再エンコードしない `-c copy` を並列化してもディスク I/O を奪い合うだけでスループットが上がらないため (`allaganeye/commands/export.py`) |
```

- [ ] **Step 4: §キャッシュ再読み込み失敗時のフォールバック の関数名を直す (#879)**

`_load_cache` は #879 で廃止され `_load_cache_hit` に統合された (`allaganeye/commands/split_matches.py:2284`)。**振る舞いの記述は正しいまま**なので、関数名のみ直す。

現状 (`docs/cli-spec.md:146`):

```markdown
`_load_cache` 検証通過後でも race condition / 破損 / 権限変更等で helper 側の読み直しが失敗しうる。
```

置換後:

```markdown
`_load_cache_hit` 検証通過後でも race condition / 破損 / 権限変更等で helper 側の読み直しが失敗しうる。
```

- [ ] **Step 5: 実装と突合する**

Run:

```bash
python -m allaganeye detect --help
python -m allaganeye export --help
grep -rn "def _load_cache" allaganeye/commands/split_matches.py
```

Expected: 表に載せた option がすべて `--help` に現れ、`--vtuber` は現れない。`_load_cache_hit` のみが定義されており `_load_cache` は存在しない

- [ ] **Step 6: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 7: Commit**

```bash
git add docs/cli-spec.md
git commit -m "docs(#862): cli-spec の option 表に --masked / --gpu-vendor / --progress-format と copy 時の concurrency 規則を反映、_load_cache_hit へ改名追従 (Refs #862)"
```

---

### Task 5: `docs/cli-spec.md` の metadata トップレベル表に欠落 7 field を追加する (P3)

**Files:**

- Modify: `docs/cli-spec.md` `### metadata.json` の **トップレベル:** 表

**参照:** `schemas/metadata.schema.json` の `properties` は 16 field。doc の表は 9 field しか持たない。

- [ ] **Step 1: 表の直前に抜粋である旨の注記を追加する**

現状:

```markdown
**トップレベル:**
```

置換後:

```markdown
**トップレベル:** (`schemas/metadata.schema.json` の全 16 field を掲載。フィールドごとの詳細契約は [`docs/metadata-spec.md`](metadata-spec.md) が正)
```

- [ ] **Step 2: 表に欠落 7 field を追加する**

`|`source`| string | 入力動画のファイルパス |` の**直前**に追加:

```markdown
| `schema_version` | string | metadata スキーマの版数 (現行 `"1"`)。欠落は v1 として読む (#515) |
```

`|`source_duration_display`| string | 総再生時間の表示形式（MM:SS or H:MM:SS） |` の**直後**に追加:

```markdown
| `source_fps` | float | 入力動画のフレームレート。存在する場合は必ず float (省略可能、pre-0.2.0 legacy metadata.json では欠落する場合があり、読み込み側は欠落時 DEFAULT_FPS=60 で補完する) |
```

> **実行中の訂正 (2026-07-31)**: 当初 plan は `float \| null` としていたが、`schemas/metadata.schema.json:37-41` (`"type": "number"`、null なし) / `allaganeye/metadata_types.py:141` (`NotRequired[float]`) / writer 実装のいずれも `null` を出力しないことを実測確認したため、上記の通り訂正した (commit `2c2787a`)。

`|`gaps`| array | 試合間の有意なギャップ（>=5分） |` の**直後**に追加:

```markdown
| `warnings` | array | 検知時の警告エントリ (#518)。現行は常に空配列 |
| `system_info` | object | 検知実行環境の記録 (#591)。GPU vendor は export のエンコーダ選択に使われる |
| `brightness_samples` | object | GUI タイムライン描画用の輝度サンプル (#569) |
| `capture_regions` | object | 解決された検出 ROI とその縮退 provenance (#810) |
| `minimap_regions` | array | `minimap` コマンドが書き戻すエリアマップ切抜き領域 (#481) |
```

- [ ] **Step 3: 表の field 数を schema と突合する**

Run:

```bash
PYTHONUTF8=1 python -c "import json;print(len(json.load(open('schemas/metadata.schema.json',encoding='utf-8'))['properties']))"
```

Expected: `16` — doc の表も 16 行になっていること

- [ ] **Step 4: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 5: Commit**

```bash
git add docs/cli-spec.md
git commit -m "docs(#862): cli-spec の metadata トップレベル表に欠落 7 field を追加 (Refs #862)"
```

---

### Task 6: `docs/tuning-guide.md` の workers / GPU default を直す (P2-25 / P2-28)

**Files:**

- Modify: `docs/tuning-guide.md:173` / `:249` / `:250`

- [ ] **Step 1: 本文 (`:173`) を直す**

現状:

```markdown
デフォルトでは CPU コア数（最大 24）が自動設定されます。通常は変更不要ですが、他のプロセスとリソースを共有する場合は下げることも検討してください。
```

置換後:

```markdown
デフォルトでは CPU コア数に応じたワーカー数が自動設定されます（上限は実装側の cap に従います。正は `allaganeye/video/detector.py` の `_resolve_workers` docstring）。通常は変更不要ですが、他のプロセスとリソースを共有する場合は下げることも検討してください。
```

- [ ] **Step 2: パラメータ表 (`:249`) を直す**

現状:

```markdown
| `--workers` | auto | 1 以上 | 並列ワーカー数。デフォルトは CPU コア数（最大 24） |
```

置換後:

```markdown
| `--workers` | auto | 1 以上 | 並列ワーカー数。auto は CPU コア数から実装の cap で解決される (正: `_resolve_workers` docstring) |
```

- [ ] **Step 3: GPU default (`:250`) を直す**

現状:

```markdown
| `--gpu` / `--no-gpu` | `--no-gpu` | - | GPU アクセラレーション検知。利用不可時は CPU フォールバック |
```

置換後:

```markdown
| `--gpu` / `--no-gpu` | auto | - | GPU アクセラレーション検知。**どちらも指定しない場合はコーデックから自動選択** (H.264/HEVC/AV1/VP9 → GPU、それ以外 → CPU、#414)。利用不可時は CPU フォールバック |
```

- [ ] **Step 4: 実装と突合する**

Run:

```bash
grep -n "use_gpu" allaganeye/config.py
```

Expected: default が `None` (= auto) であり `False` ではないこと

- [ ] **Step 5: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 6: Commit**

```bash
git add docs/tuning-guide.md
git commit -m "docs(#862): tuning-guide の workers 上限を SSoT 参照化、GPU default を auto に訂正 (Refs #862)"
```

---

### Task 7: `docs/benchmarks.md` §CPU コア数 の仕様主張のみ SSoT 化する (P2-25)

**Files:**

- Modify: `docs/benchmarks.md:78-79`

**注意:** `benchmarks.md:28` `:36` `:50` `:58` は §性能改善の推移 の**計測記録**であり、**1 文字も変更しない**。

- [ ] **Step 1: §CPU コア数 の 2 行を直す**

現状:

```markdown
- `max_workers` のデフォルトは `min(cpu_count, 24)`
- 32コア環境: workers=24 で十分な並列度（残りのコアは OS + ffmpeg プロセスに使用）
```

置換後:

```markdown
- `max_workers` のデフォルトは CPU コア数と実装側の cap から解決される (正: `allaganeye/video/detector.py` の `_resolve_workers` docstring)
- 多コア環境では cap で頭打ちになる。残りのコアは OS + ffmpeg プロセスに使われる（下の §性能改善の推移 の数値は当時の cap 24 で計測したもの）
```

- [ ] **Step 2: 実測記録が無傷であることを確認する**

Run:

```bash
git diff docs/benchmarks.md
```

Expected: diff に現れる行は上記 2 行のみ。`:28` `:36` `:50` `:58` は現れない

- [ ] **Step 3: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 4: Commit**

```bash
git add docs/benchmarks.md
git commit -m "docs(#862): benchmarks の workers 仕様主張のみ SSoT 参照化 (実測記録は保持、Refs #862)"
```

---

### Task 8: `docs/output-spec.md` の適用範囲に `detect` を追加する (P3)

**Files:**

- Modify: `docs/output-spec.md:3` / `:8`

- [ ] **Step 1: リード文 (`:3`) を直す**

現状:

```markdown
`allaganeye split` コマンドの CLI オプション組み合わせごとの**期待出力**を定義する。実装と docs の整合性を検証する基準として使用し、新規 CLI オプションを追加する PR は本マトリクスに行・列を追加することを必須とする (#405)。
```

置換後:

```markdown
`allaganeye split` / `allaganeye detect` コマンドの CLI オプション組み合わせごとの**期待出力**を定義する。実装と docs の整合性を検証する基準として使用し、新規 CLI オプションを追加する PR は本マトリクスに行・列を追加することを必須とする (#405)。
```

- [ ] **Step 2: 適用範囲 (`:8`) を直す**

現状:

```markdown
- **対象コマンド**: `allaganeye split`
```

置換後:

```markdown
- **対象コマンド**: `allaganeye split` / `allaganeye detect` (#463 で分離。detect は split の検知フェーズと同じ進捗・verbose 出力契約に従う)
```

- [ ] **Step 3: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 4: Commit**

```bash
git add docs/output-spec.md
git commit -m "docs(#862): output-spec の適用範囲に detect を追加 (Refs #862)"
```

---

### Task 9: `docs/developer-setup.md` の ffmpeg インストール手順を LGPL 優先にする (P2-35)

**Files:**

- Modify: `docs/developer-setup.md` §「インストール方法」の **Windows** ブロック

**問題:** 同 doc は「推奨: ffmpeg 8.1 LGPLv3」と書きながら、インストール手順は GPL ビルドの `winget install Gyan.FFmpeg` を「（推奨）」と表記しており自己矛盾している (#508 方針と不整合)。

- [ ] **Step 1: Windows ブロックを書き換える**

現状:

````markdown
**Windows**（いずれか 1 つ）:

```bash
# winget（推奨）
winget install Gyan.FFmpeg

# scoop
scoop install ffmpeg

# Chocolatey
choco install ffmpeg
```

パッケージマネージャを使わない場合は [gyan.dev](https://www.gyan.dev/ffmpeg/builds/) から `ffmpeg-release-essentials.zip` をダウンロードし、展開先の `bin/` フォルダを PATH に追加してください。
````

置換後:

````markdown
**Windows**:

**推奨: BtbN の LGPLv3 ビルド** (CI / Portable ZIP と同一系列、#508)。[BtbN/FFmpeg-Builds/releases](https://github.com/BtbN/FFmpeg-Builds/releases) の monthly snapshot から `ffmpeg-n8.1-*-win64-lgpl-shared*.zip` (実アセット名は `ffmpeg-n8.1-11-g75d37c499d-win64-lgpl-shared-8.1.zip` のように末尾に snapshot 系列が付く場合がある) をダウンロードして任意のフォルダに展開し、`ALLAGANEYE_FFMPEG` にその `bin/` を指定します:

```bash
# 現在のセッションのみ
set ALLAGANEYE_FFMPEG=C:\path\to\ffmpeg-n8.1-11-g75d37c499d-win64-lgpl-shared-8.1\bin
```

パッケージマネージャ経由でも動作しますが、いずれも **GPL ビルド**であり CI / Portable ZIP 同梱版 (LGPLv3) とライセンス系列が異なります。手軽さを優先する場合のみ使ってください:

```bash
# winget (GPL ビルド)
winget install Gyan.FFmpeg

# scoop (GPL ビルド)
scoop install ffmpeg

# Chocolatey (GPL ビルド)
choco install ffmpeg
```

`winget` のインストール先は自動検索されるため、`Gyan.FFmpeg` を使う場合 PATH 設定は通常不要です (後方互換、`allaganeye/ffmpeg_path.py`)。
````

> **実行中の訂正 (2026-07-31)**: 当初 plan は glob を `ffmpeg-n8.1-*-win64-lgpl-shared.zip`、パス例を `ffmpeg-n8.1-win64-lgpl-shared` としていたが、BtbN の実アセット名は末尾に snapshot 系列が付く (`scripts/build-portable-zip.ps1:87` の pin 値 = `ffmpeg-n8.1-11-g75d37c499d-win64-lgpl-shared-8.1`) ため、上記の通り訂正した (commit `bbf42b4` / `f6e1a8c`)。

- [ ] **Step 2: 自動検索の記述と矛盾しないことを確認する**

Run:

```bash
grep -n "winget\|ALLAGANEYE_FFMPEG" allaganeye/ffmpeg_path.py
```

Expected: winget のインストール先探索と `ALLAGANEYE_FFMPEG` 環境変数の両方が実装されている

- [ ] **Step 3: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 4: Commit**

```bash
git add docs/developer-setup.md
git commit -m "docs(#862): developer-setup の ffmpeg 手順を BtbN LGPL 優先へ (#508 方針と整合、Refs #862)"
```

---

### Task 10: `docs/coding-conventions.md` §背景 の引用値を実測に合わせる

**Files:**

- Modify: `docs/coding-conventions.md:91`

**根拠:** 上位 spec §W6 の突合指示。監査 headline「6 doc 7 箇所」は実測 (4 doc 15 箇所、うち仕様主張 8 箇所) と食い違う。

- [ ] **Step 1: §背景 の 1 文目を直す**

現状:

```markdown
workers 上限「24」が 6 doc 7 箇所に複製されたまま実装 (32) と drift した (2026-06-10 full audit P2-25)。同種の doc–実装 drift が P2-26〜P2-28 等でも反復しており、値の複製自体が drift の構造的な再発要因。詳細は [`docs/audits/2026-06-10-full-audit.md`](audits/2026-06-10-full-audit.md) および [audit-remediation spec §N3](superpowers/specs/2026-06-10-audit-remediation-design.md#n3-doc-ssot-規約の明文化--release-gate-追記) を参照。
```

置換後:

```markdown
workers 上限「24」が 4 doc 15 箇所 (うち仕様主張は 8 箇所、残る 7 箇所は当時の計測値を記録した実測記録) に複製されたまま実装 (32) と drift した (2026-06-10 full audit P2-25。監査 headline の「6 doc 7 箇所」は #862 の再突合で実測と不一致であることが判明した)。同種の doc–実装 drift が P2-26〜P2-28 等でも反復しており、値の複製自体が drift の構造的な再発要因。**実測記録は書き換えない** — 行われていない測定条件を記録することになるため、SSoT 化の対象は仕様を主張している箇所に限る。詳細は [`docs/audits/2026-06-10-full-audit.md`](audits/2026-06-10-full-audit.md) および [audit-remediation spec §N3](superpowers/specs/2026-06-10-audit-remediation-design.md#n3-doc-ssot-規約の明文化--release-gate-追記) を参照。
```

- [ ] **Step 2: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 3: Commit**

```bash
git add docs/coding-conventions.md
git commit -m "docs(#862): coding-conventions §背景 の workers drift 実測値を訂正 (Refs #862)"
```

---

### Task 11: PR-A の全体ゲートと PR 作成

**Files:** (変更なし。検証と PR 作成のみ)

- [ ] **Step 1: 全 repo ゲートを引数なしで回す**

Run:

```bash
python -m ruff check .
python -m ruff format --check .
python -m pyright
pytest
bash scripts/check-markdownlint.sh
```

Expected: すべて pass。`pytest` は `slow` マーカー除外の通常実行

- [ ] **Step 2: 実測記録 7 箇所が無傷であることを最終確認する**

Run:

```bash
git diff origin/develop-0.3.0 -- docs/benchmarks.md docs/cli-spec.md | grep "^-" | grep "24"
```

Expected: 出力は Task 3 / Task 7 で SSoT 化した仕様主張の行のみ。`allaganeye 0.1.1` サンプル行 (`workers=auto (24)`) と §性能改善の推移 の行は削除側に現れない

- [ ] **Step 3: PR Pre-flight (Iron Law 6)**

Run:

```bash
gh pr list --search "862" --state open
git fetch origin develop-0.3.0
git log HEAD..origin/develop-0.3.0 --oneline
gh pr list --search "862" --state all
```

Expected: Step 0 で並行 PR なし。base に未取り込み commit があれば取り込んでから進む

- [ ] **Step 4: Codex adversarial-review (Pre-flight Step 5)**

companion script の `adversarial-review` subcommand を `--background` で起動し、focus に `Iron Law 3 scope creep / doc-implementation drift claims verified against source / SSoT canonical location correctness / preserved measurement records not falsified` を渡す (ASCII)。

- [ ] **Step 5: PR を作成する**

`develop-0.3.0` を base に PR-A を作成する。本文は `printf | gh pr create --body-file -` で渡す (日本語 UTF-8 破損回避)。`Refs #862` のみを書き、`Closes` は使わない。Self-Test Report で machine-verified を `[x]`、machine-unverifiable を plain bullet `-` で書き分ける。

- [ ] **Step 6: merge を Idios に依頼する**

`AskUserQuestion` で PR-A の merge を依頼する。**agent は merge しない**。PR-B は PR-A の merge 後に着手する。

---

## PR-B: アーキテクチャ説明系

> PR-B は PR-A が `develop-0.3.0` に merge された後、`git fetch origin develop-0.3.0` で最新を取り込んだ新ブランチで着手する。

### Task 12: `CLAUDE.md` のモジュール表に欠落モジュールを追加する (P2-29)

**Files:**

- Modify: `CLAUDE.md` §モジュール構成 の表

**欠落の実測:** `allaganeye/` 実ファイルと表を突合した結果、以下が表に無い。

- [ ] **Step 1: 欠落を再確認する**

Run:

```bash
ls allaganeye/ allaganeye/commands/ allaganeye/export/ allaganeye/detection/ allaganeye/tools/
```

Expected: `integrity.py` / `metadata_types.py` / `system_info.py` / `commands/encoder_slots.py` / `commands/export.py` / `export/` 6 file / `video/vtuber_timeline.py` / `detection/migrations.py` / `detection/progress_emitter.py` / `detection/warnings.py` / `tools/regen_audio_refs.py` が存在する

- [ ] **Step 2: `ffmpeg_path.py` 行の直後に 3 行を追加する**

```markdown
| `system_info.py` | `-v` verbose ヘッダ用のハード情報取得 (#377) + GPU vendor probe (`probe_gpu_vendors`)。全 public helper は失敗時に `"(unavailable)"` を返し例外を投げない |
| `integrity.py` | Portable ZIP 同梱物の整合性検査 (#668)。`integrity-manifest.json` を読み CLI `--version` で検証 (exit 7)。Rust 側 `gui/src-tauri/src/integrity.rs` が同ロジックをミラー |
| `metadata_types.py` | **自動生成** (`python scripts/codegen/generate.py`、#612)。`schemas/metadata.schema.json` から TypedDict を生成。手編集禁止 |
```

- [ ] **Step 3: `commands/minimap.py` 行の直後に 2 行を追加する**

```markdown
| `commands/export.py` | export コマンド (#761)。metadata.json (positional または `--stdin`) を読み、エンコーダスロットを列挙して並列 export を実行。`--json` で JSON Lines 進捗を emit |
| `commands/encoder_slots.py` | 隠しコマンド `encoder-slots` (#761)。GUI Tauri `enumerate_h264_encoders` が subprocess で呼び、EncoderSlot 一覧を JSON で返す |
```

- [ ] **Step 4: `video/splitter.py` 行の直後に export サブパッケージ 6 行を追加する**

```markdown
| `export/encoder.py` | H.264 エンコーダ選択ロジック (#761)。GUI Rust 側から移植し CLI / GUI で単一の正を共有 |
| `export/nvenc_probe.py` | SKU テーブルによる NVENC 物理エンジン数の推定 (#761)。live な `nvidia-smi` probe は不採用 |
| `export/pool.py` | 並列 export オーケストレータ (#761)。`ThreadPoolExecutor` で N ワーカーを起動し、`cancel_event` で in-flight ffmpeg を kill |
| `export/ffmpeg_runner.py` | 単一 match の ffmpeg 起動 + libx264 fallback retry (#761)。NVDEC/NVENC 失敗パターン検知は `_nvenc_decode_stage_failure` (#791 / #899) |
| `export/schema.py` | stdout JSON Lines の wire protocol dataclass (#761)。Rust `start_export` が `ExportProgress` イベントへ変換 |
| `export/wire.py` | thread-safe な stdout JSON Lines emitter (#761)。`threading.Lock` で 1 行 emit の原子性を保証 |
```

- [ ] **Step 5: `video/probe_state.py` 行の直後に 1 行を追加する**

```markdown
| `video/vtuber_timeline.py` | VTuber presence x motion timeline 検出 (#895)。`--vtuber` 専用の境界候補 generator。OBS / masked path からは import されない |
```

- [ ] **Step 6: `detection/` 行を書き換え、`tools/` 行を追加する**

現状:

```markdown
| `detection/` | 検知パイプラインの共有ヘルパ (#463)。`format.py` (フォーマッタ) / `metadata_writer.py` (atomic read/write) |
```

置換後:

```markdown
| `detection/` | 検知パイプラインの共有ヘルパ (#463)。`format.py` (フォーマッタ) / `metadata_writer.py` (atomic read/write) / `migrations.py` (`schema_version` 検証 + 前方 migration、#515) / `progress_emitter.py` (`--progress-format json` の JSON Lines emitter、#569) / `warnings.py` (metadata `warnings` の payload scaffold、#518) |
| `tools/regen_audio_refs.py` | 同梱参照特徴量 (`audio/refs/*.npz`) をメンテナ手元の録画から再生成する開発用スクリプト |
```

- [ ] **Step 7: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 8: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(#862): CLAUDE.md モジュール表に欠落 17 モジュールを追加 (Refs #862)"
```

---

### Task 13: `CLAUDE.md` の §コマンド / L2 状態 / データフロー / #860 を直す (P2-26 / P2-29 / P2-30 / 新規)

**Files:**

- Modify: `CLAUDE.md` §段階的アーキテクチャ の L2 行 / §コマンド の CLI ブロック / §データフロー / §GPU モード

- [ ] **Step 1: L2 の状態を「リリース済み」にする (P2-30)**

現状:

```markdown
| L2: 配布・統合 | GUI + ゼロ環境構築配布 | Tauri 2.x + React 19 + TS | **開発中** |
```

置換後:

```markdown
| L2: 配布・統合 | GUI + ゼロ環境構築配布 | Tauri 2.x + React 19 + TS | **リリース済み** (v0.2.0 / v0.2.1) |
```

- [ ] **Step 2: §コマンド の CLI ブロックに `export` を追加する (P2-29)**

現状:

```bash
allaganeye minimap <metadata.json>                               # エリアマップ window 領域を提案（exit 4）
allaganeye minimap <metadata.json> --region X,Y,W,H [-o DIR]    # 指定領域で crop + H.264 encode
```

置換後:

```bash
allaganeye minimap <metadata.json>                               # エリアマップ window 領域を提案（exit 4）
allaganeye minimap <metadata.json> --region X,Y,W,H [-o DIR]    # 指定領域で crop + H.264 encode
allaganeye export <metadata.json> -o <dir>                       # metadata から並列書き出し (#761、default codec=copy)
allaganeye export <metadata.json> -o <dir> --codec h264           # H.264 再エンコード（NVENC/QSV/AMF/libx264 を自動選択）
```

- [ ] **Step 3: §データフロー の Pass 1 記述を実装に合わせる (P2-26)**

現状:

```text
MP4/MKV入力 → probe.py（ffprobe でメタデータ取得）
           → detector.py（ffmpeg 並列 -ss プローブで暗転検知 → 試合境界タイムスタンプ）
           → splitter.py（FFmpeg -c copy で無劣化分割）
           → 出力: 試合ごとのMP4 + metadata.json
```

置換後:

```text
MP4/MKV入力 → probe.py（ffprobe でメタデータ取得）
           → detector.py（チャンク分割デコードで暗転検知 → 試合境界タイムスタンプ）
           → splitter.py（FFmpeg -c copy で無劣化分割）
           → 出力: 試合ごとのMP4 + metadata.json
```

- [ ] **Step 4: §モジュール構成 の `video/detector.py` 行を直す (P2-26)**

現状:

```markdown
| `video/detector.py` | ffmpeg 並列プローブで暗転検知、試合境界抽出（CPU モード） |
```

置換後:

```markdown
| `video/detector.py` | チャンク分割デコードで暗転検知、試合境界抽出（CPU モード、#214 以降）。Pass 2 の精密計測のみ per-frame `-ss` プローブを使う |
```

- [ ] **Step 5: §検知アルゴリズム の Pass 1 手順 2-3 を実装に合わせる (P2-26)**

実装 (`allaganeye/video/detector.py` の `_scan_cpu`) は動画をチャンクに分け、各チャンクを 1 プロセスの ffmpeg で dual seek + `select` filter デコードする。タイムスタンプごとの `-ss` プロセス起動は **Pass 2 の精密計測のみ**。

現状:

```markdown
2. 各タイムスタンプで `ffmpeg -threads 1 -ss {t} -i` により 1 フレームを 320x180 grayscale でデコード
3. `ThreadPoolExecutor(max_workers=min(cpu_count, 32))` で並列実行
```

置換後:

```markdown
2. 動画をチャンクに分割し、各チャンクをそれぞれ 1 プロセスの ffmpeg で dual seek + `select='not(mod(n,N))'` filter によりデコード。フレームは 320x180 grayscale (#214 / #576)
3. `ThreadPoolExecutor` で並列実行。チャンク数は `_scan_cpu`、ワーカー数上限は `_resolve_workers` が解決する (正: `allaganeye/video/detector.py`)
```

**補足:** 手順 3 の `min(cpu_count, 32)` は値としては実装と一致しているが、SSoT 規約が潰そうとしている「値の複製」そのものであり、この行を P2-26 で書き換える機会に参照へ寄せる。監査が数えた仕様主張 8 箇所には含まれない (値が 24 ではなく 32 のため監査の grep に掛からなかった) が、同じ drift クラスであることを明示して直す。チャンク数も同じ理由で数値を書かず `_scan_cpu` への参照にする (Task 15 の `docs/video-processing.md` 側と同一方針)。

- [ ] **Step 6: §GPU モード に #860 の CIM fallback を追記する**

`probe_gpu_vendors()` の実装を確認する:

```bash
grep -n "CIM\|Get-CimInstance\|wmic" allaganeye/system_info.py
```

確認した実装に合わせ、§GPU モード の vendor 自動選択の箇所 (`_VENDOR_PREFERENCE` を説明する行) の**直後**に次を追加する:

```markdown
- Windows の GPU 列挙は `wmic` を第一候補とし、`wmic` 非搭載環境 (Windows 11 24H2 以降で既定削除) では PowerShell `Get-CimInstance Win32_VideoController` にフォールバックする (#860)。両方失敗した場合は vendor 検出なし = CPU モードに縮退する
```

- [ ] **Step 7: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 8: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(#862): CLAUDE.md の L2 状態 / export コマンド / Pass 1 方式 / #860 CIM fallback を反映 (Refs #862)"
```

---

### Task 14: `docs/design-overview.md` の L2 状態を直す (P2-30)

**Files:**

- Modify: `docs/design-overview.md:18`

- [ ] **Step 1: レイヤー図の L2 行を直す**

現状:

```text
│  L2: 配布・統合（開発中）                            │
```

置換後 (**罫線の桁揃えを崩さないこと**。`開発中` 3 文字を `v0.2.1` 6 文字相当に置き換えるため、末尾の空白を調整する):

```text
│  L2: 配布・統合（リリース済み v0.2.1）               │
```

- [ ] **Step 2: 図の桁が揃っていることを目視確認する**

Run:

```bash
sed -n '10,50p' docs/design-overview.md
```

Expected: `│` の右端が上下の行と揃っている

- [ ] **Step 3: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 4: Commit**

```bash
git add docs/design-overview.md
git commit -m "docs(#862): design-overview の L2 状態をリリース済みに更新 (Refs #862)"
```

---

### Task 15: `docs/video-processing.md` の Pass 1 / #576 / workers を直す (P2-26 / P2-27 / P2-25)

**Files:**

- Modify: `docs/video-processing.md:8` (§概要) / `:36-49` (§フレームサンプリング方式) / `:51-55` (§並列実行) / `:124` (§CPU モードとの差異) / `:136-140` (#576 新 path) / `:380-381` (実測記録への注記)

**注意 1:** `:380` `:381` は §性能チューニング の**実測記録**。数値 (`8 → min(cpu_count, 24)` / `~3x` / `768 スレッド`) は**書き換えない**。注記を足すだけ。

**注意 2:** `:302` の「非採用案の比較表」(`ffmpeg 並列 -ss プローブ（採用）` を含む) は **PR #21 時点の設計判断記録**であり、当時どの案を選んだかの史実。**書き換えない**。`:55` の §設計経緯 も履歴なので、書き換えではなく現在までの続きを追記する。

**実装の根拠:** `allaganeye/video/detector.py` の `_scan_cpu` は `num_chunks = min(os.cpu_count() or 4, 32)` でチャンクに分け、各チャンクを `_decode_chunk_cpu` (dual seek + `select` filter) で 1 プロセスずつデコードする。タイムスタンプごとに ffmpeg を起動する `-ss` プローブ方式は **Pass 2 の精密計測と各種 helper のみ**で、Pass 1 では #214 以降使っていない。

- [ ] **Step 1: §概要 (`:8`) の detect 説明を直す (P2-26)**

現状:

```markdown
2. **detect**: ffmpeg 並列プローブで暗転を検知し、試合境界を特定
```

置換後:

```markdown
2. **detect**: ffmpeg のチャンク並列デコードで暗転を検知し、試合境界を特定
```

- [ ] **Step 2: §フレームサンプリング方式 (`:36-49`) を実装に合わせる (P2-26)**

現状:

````markdown
全フレームの解析はコストが高いため、一定間隔（デフォルト1秒）でフレームをサンプリングする。

**方式**: ffmpeg の `-ss`（入力シーク）で各タイムスタンプに直接ジャンプし、1フレームのみデコード。

```bash
ffmpeg -ss {timestamp} -i input.mkv -frames:v 1 -s 320x180 -pix_fmt gray -f rawvideo pipe:1
```

- `-ss` を `-i` の前に配置（入力シーク = キーフレームベースの高速シーク）
- `-s 320x180`: 輝度計算に十分な低解像度（デコード負荷 1/36）
- `-pix_fmt gray`: grayscale でパイプ出力（Python 側の変換不要）
- パイプから `numpy.frombuffer` + `numpy.mean` で平均輝度を計算
````

置換後:

````markdown
全フレームの解析はコストが高いため、一定間隔（デフォルト1秒）でフレームをサンプリングする。

**方式（Pass 1、#214 以降）**: 動画を CPU コア数に応じた数のチャンクに分割し、チャンクごとに 1 つの ffmpeg プロセスで dual seek + `select` filter デコードする（正: `allaganeye/video/detector.py` の `_scan_cpu` → `_decode_chunk_cpu`）。

```bash
ffmpeg -threads 1 -ss {input_seek} -i input.mkv -ss {output_seek} -t {chunk_duration} \
  -fps_mode passthrough \
  -vf "select='not(mod(n\,{N}))',scale=320:180,format=gray" \
  -f rawvideo -pix_fmt gray pipe:1
```

- 入力 `-ss`（`-i` の前）: `chunk_start - SEEK_LEAD_SECONDS` 付近のキーフレームへ高速ジャンプ（デコードなし）
- 出力 `-ss`（`-i` の後）: GOP プリロールをフレーム単位で正確にトリムし、フィルタグラフの先頭を `chunk_start` に合わせる
- `select='not(mod(n\,N))'`: **フレームインデックス** `n` ベースで N 枚おきに抽出（PTS ベースの `fps` filter と違い version 非依存）。`N = round(sample_interval * fps_num / fps_den)`
- `scale=320:180`: 輝度計算に十分な低解像度（デコード負荷 1/36）
- `format=gray` + `-pix_fmt gray`: grayscale でパイプ出力（Python 側の変換不要）
- パイプから `numpy.frombuffer` + `numpy.mean` で平均輝度を計算。emit されたフレーム K をチャンクのタイムスタンプ K 番目に位置対応で割り当てる

**方式（Pass 2 / 各種 helper）**: タイムスタンプ単位の再プローブは今も入力シーク 1 フレームデコードを使う。

```bash
ffmpeg -ss {timestamp} -i input.mkv -frames:v 1 -s 320x180 -pix_fmt gray -f rawvideo pipe:1
```
````

- [ ] **Step 3: §並列実行 (`:51-55`) を実装に合わせ workers を SSoT 化する (P2-26 / P2-25)**

現状:

```markdown
`ThreadPoolExecutor(max_workers=min(cpu_count, 24))` で複数タイムスタンプを同時にプローブ。各 ffmpeg プロセスは独立したキーフレームシークを行うため、不要フレームのデコードが発生しない。`--workers` オプションで明示指定も可能。

**設計経緯**: OpenCV `VideoCapture` → シーケンシャル `grab()`/`read()` → ffmpeg `select` フィルタ → **並列 `-ss` プローブ** と段階的に改善。select フィルタは全フレームをデコード後にフィルタリングするため、大容量ファイルで効果がなかった。
```

置換後 (§設計経緯 は既存の記述を保持し、その後の変遷を追記する):

```markdown
`ThreadPoolExecutor` で複数チャンクを同時にデコードする。ワーカー数は `_resolve_workers` が CPU コア数と実装側の cap から解決する (正: `allaganeye/video/detector.py` の `_resolve_workers` docstring)。`--workers` オプションで明示指定も可能。チャンク数がワーカー数を上回る場合は wave 実行になる。

**設計経緯**: OpenCV `VideoCapture` → シーケンシャル `grab()`/`read()` → ffmpeg `select` フィルタ → **並列 `-ss` プローブ** と段階的に改善。select フィルタは全フレームをデコード後にフィルタリングするため、大容量ファイルで効果がなかった。その後 **チャンク分割デコード** (#214、プロセス起動コストとシークオーバーヘッドの削減) → **dual seek + フレームインデックスベース `select` filter** (#576、下記 §ffmpeg fps filter の version 依存制約) へ移行している。かつて非採用とした `select` フィルタが復活しているのは、当時の「全フレームをデコード後にフィルタリング」という問題が、チャンク境界を入力シークで絞ることで解消されたため。
```

- [ ] **Step 4: §CPU モードとの差異 (`:124`) を直す (P2-26)**

現状:

```markdown
**CPU モードとの差異**: CPU モードはタイムスタンプごとに独立した ffmpeg プロセスを起動する（`-ss` プローブ方式）。GPU モードは少数の長寿命プロセスでチャンク全体をデコードする。Pass 1 以降（transition expansion, Pass 2, フィルタリング）は共通。
```

置換後:

```markdown
**CPU モードとの差異**: CPU / GPU いずれもチャンク分割デコードだが、GPU モードは `-hwaccel` によるハードウェアデコードを使い、チャンク数を動画長に応じて動的調整する (#437) 点が異なる。CPU モードのチャンク数は CPU コア数のみで決まる (正: `_scan_cpu`)。Pass 1 以降（transition expansion, Pass 2, フィルタリング）は共通。
```

- [ ] **Step 5: #576 新 path の記述を実装に合わせる (`:136-140`)**

現状:

```markdown
**新 path (#576 完了後、default)** は fps filter を廃止し、output seek
(`-ss` を `-i` の後ろ) + `-fps_mode passthrough` + Python 側 N-th
sampling (`frame_idx = round((t - chunk_start) * fps_num / fps_den)`) で
frame を選択する。ffmpeg 内部 frame-rate normalization の version 依存を
構造的に escape する設計。
```

置換後 (実装は `detector.py` の `dual seek + select filter + -fps_mode passthrough`):

```markdown
**新 path (#576 完了後、default)** は fps filter を廃止し、**dual seek**
(input seek で `SEEK_LEAD_SECONDS` 手前まで飛び、output seek で chunk 先頭に
合わせる) + ffmpeg の `select` filter (`select='not(mod(n\,N))'`、frame index
`n` ベースの N 枚おき抽出) + `-fps_mode passthrough` で frame を選択する。
時刻ではなく **frame index** で選ぶため、ffmpeg 内部の frame-rate
normalization の version 依存を構造的に escape する。
```

- [ ] **Step 6: 実装と突合する**

Run:

```bash
grep -n "select='not(mod\|SEEK_LEAD_SECONDS\|num_chunks = " allaganeye/video/detector.py
```

Expected: `select='not(mod(n\,{n_step}))'` を組み立てる箇所 / `SEEK_LEAD_SECONDS` 定数 / `_scan_cpu` の `num_chunks = min(os.cpu_count() or 4, 32)` がすべて見つかる

- [ ] **Step 7: §性能チューニング 表に計測時点の注記を足す (`:380-381`)**

表の**直後**に次の 1 行を追加する (表の行自体は変更しない):

```markdown
> 上表の `min(cpu_count, 24)` / `workers=24` は**計測当時 (PR #57 / #69) の cap** による値。現行の cap は `_resolve_workers` docstring を参照 (#862)。計測条件を保つため数値は当時のまま残している。
```

- [ ] **Step 8: 実測記録の数値が無傷であることを確認する**

Run:

```bash
git diff docs/video-processing.md | grep "^-" | grep "24"
```

Expected: §並列実行 (`:53`) の 1 行のみ。`:380` `:381` と `:302` の比較表は削除側に現れない

- [ ] **Step 9: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 10: Commit**

```bash
git add docs/video-processing.md
git commit -m "docs(#862): video-processing の Pass 1 をチャンク分割デコードに訂正、#576 新 path を dual seek + select filter に訂正、workers を SSoT 参照化 (Refs #862)"
```

---

### Task 16: `docs/scorebar-detection-design.md` を #803 / #797 / #811 に同期する (P2-31)

**Files:**

- Modify: `docs/scorebar-detection-design.md` §動的 emblem 位置 の項目 1 / §閾値定数 表 / §残課題 の直前

**問題:** doc は「saturated 列の**最長 run**」と書いているが、実装 (#803) は「screen center を**跨ぐ** run」を選ぶ。最長 run 選択は #803 が明示的に否定した挙動であり、doc は実装と**逆**。

- [ ] **Step 1: span 検出の記述を直す**

現状:

```markdown
1. `_find_scorebar_horizontal_range(raw_rgb)`: 画面上部 y=0..45 の HSV saturation mask で saturated 列の最長 run を scorebar span として返す。run 幅 < 500px は None (lobby UI 誤検出排除)
```

置換後:

```markdown
1. `_find_scorebar_horizontal_range(raw_rgb)`: 画面上部 y=0..45 の HSV saturation mask で saturated 列の run を構築し、**画面中央 (x=960) を跨ぐ run** を scorebar span として返す (#803)。FL scorebar は水平中央に配置されるため、**最長 run を選ぶと**より長い off-center / 過大幅のバンド (右側チャットパネル、post-match の色鮮やかな屋内など) が有効な中央 scorebar を隠してしまい、rescue path が支えるはずの 4K / HUD-scaled layout を false-negative にする。中央を跨ぐ run が無い場合、および span 幅が 500px 未満 / 1440px 超の場合は None
```

- [ ] **Step 2: §閾値定数 表に幅上限を追加する**

`|`_SCOREBAR_SCAN_MIN_WIDTH_PX`| 500 | scorebar と認める最小幅 (1080p 712+, 4K DVR 613+, lobby 409 FP 排除) |` の**直後**に追加:

```markdown
| `_SCOREBAR_SCAN_MAX_WIDTH_PX` | 1440 | scorebar と認める最大幅 (#803)。1080p OBS の実測上限 ~1090px / 4K DVR ~620px に対し、post-match の全幅近い彩度バンド (実測 ~1912px) を排除する。1440 = probe 幅 1920px の 75% |
```

- [ ] **Step 3: §残課題 の直前に #797 / #811 の節を追加する**

```markdown
### post-match trailing の flagging (#797 / #805)

末尾セグメントが動画終端まで続き `type == "unknown"` の場合、試合ではなく
post-match コンテンツ (ロビー / 街) の可能性がある。`_flag_post_match_trailing`
は候補試合ウィンドウ (`start` .. `start + min_match_duration`) を
`_TRAILING_PROBE_STRIDE` (60s) 間隔 + ウィンドウ終端で scorebar probe し、

- 1 回でも hit (`True`) → 試合映像あり → そのまま残す
- 1 回でも probe 失敗 / opencv 不在 (`None`) → 安全側でそのまま残す
- 全 probe が確定 miss (`False`) → `post_match: true` を付与して**残す**

判定は #805 段階2 で**非破壊**になった。旧 #797 はセグメントを削除していたが、
scorebar false-negative が実試合を silent に消しうるため、フラグ付与 +
default split (MP4) からの除外 + metadata 保持に置き換えられている。
セグメントが 1 個しかない場合 (blackout が 1 つも残らなかった fail-open) は
常に無変更で、「境界が見つからない」が試合ゼロに崩れることはない。

### 位置独立な scorebar 局在化 (#811)

`video/capture_region.py` の `localize_scorebar` は、上記の絶対 / 相対 2 path とは
別に、**位置を仮定しない** scorebar 局在化を提供する (L3 Phase 1 の基盤)。
saturated column run を width-gate で全件取り出し、各候補に対し 3 点 emblem AND を
評価して最も margin の大きいものを採用する。`--vtuber` gate 内および masked
fallback の at-anchor primitive (`localize_scorebar_at_anchor`、#822) として使われ、
OBS 通常 path の分類ロジックは変更しない。検出 subsystem 全体の現状 map は
[`docs/detection-map.md`](detection-map.md) を参照。
```

- [ ] **Step 4: 実装と突合する**

Run:

```bash
grep -n "_SCOREBAR_SCAN_MAX_WIDTH_PX = \|center_run\|_TRAILING_PROBE_STRIDE = " allaganeye/video/detector.py
grep -n "def localize_scorebar" allaganeye/video/capture_region.py
```

Expected: `_SCOREBAR_SCAN_MAX_WIDTH_PX = 1440` / `center_run` 選択 / `_TRAILING_PROBE_STRIDE = 60.0` / `localize_scorebar` が存在する

- [ ] **Step 5: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 6: Commit**

```bash
git add docs/scorebar-detection-design.md
git commit -m "docs(#862): scorebar-design の span 選択を #803 実装 (中央跨ぎ + 幅上限) に訂正、#797 / #811 を追記 (Refs #862)"
```

---

### Task 17: `docs/system-architecture.md` / `docs/quickstart.md` を #752 / #761 に同期する (P2-32)

**Files:**

- Modify: `docs/system-architecture.md` §2.1 表 / §2.3 表 + 直後の段落
- Modify: `docs/quickstart.md` ZIP レイアウトの `text` ブロック

**問題:** 同一 doc 内の §2.6 が「`python/` と `lib/` は #752 で廃止、PyInstaller frozen CLI」と書いているのに、§2.1 は「同梱 Python + `python -m allaganeye`」のまま。§2.3 は export を `allaganeye split --from-metadata` と書いているが、#761 で `allaganeye export` + `start_export` に変わっている。

- [ ] **Step 1: §2.1 の実体列を直す**

現状:

```markdown
| `allaganeye.bat` 引数付き (Portable ZIP) | Cmd / PowerShell で `allaganeye.bat <subcommand>` または動画ドラッグ | 同梱 Python + `python -m allaganeye` | リリース済み (v0.1.1) |
```

置換後:

```markdown
| `allaganeye.bat` 引数付き (Portable ZIP) | Cmd / PowerShell で `allaganeye.bat <subcommand>` または動画ドラッグ | PyInstaller frozen CLI `allaganeye\allaganeye.exe` (v0.3.0+ #752。v0.2.x までは同梱 Python + `python -m allaganeye`) | リリース済み (v0.1.1) |
```

- [ ] **Step 2: §2.3 の ExportScreen 行を直す**

現状:

```markdown
| ExportScreen (本物化予定) | `allaganeye split --from-metadata <meta>` | metadata.json + MP4 | [#466](https://github.com/Idios/kobutachan-allaganeye/issues/466) Phase 4 |
```

置換後:

```markdown
| ExportScreen | `allaganeye export <meta> \| --stdin -o <dir> [--codec h264] --json` | MP4 (per match) | [#466](https://github.com/Idios/kobutachan-allaganeye/issues/466) Phase 4 / [#761](https://github.com/Idios/kobutachan-allaganeye/issues/761) |
| ExportScreen (マウント時) | `allaganeye encoder-slots` | EncoderSlot 一覧 (JSON) | [#761](https://github.com/Idios/kobutachan-allaganeye/issues/761) |
```

- [ ] **Step 3: §2.3 の DetectingScreen 行から「(本物化予定)」を外す**

現状:

```markdown
| DetectingScreen (本物化予定) | `allaganeye detect <video> -o <output>` | metadata.json | [#465](https://github.com/Idios/kobutachan-allaganeye/issues/465) Phase 3 |
```

置換後:

```markdown
| DetectingScreen | `allaganeye detect <video> -o <output> --progress-format json` | metadata.json | [#465](https://github.com/Idios/kobutachan-allaganeye/issues/465) Phase 3 / [#569](https://github.com/Idios/kobutachan-allaganeye/issues/569) |
```

- [ ] **Step 4: §2.3 冒頭のリード文から「予定」を外す**

現状:

```markdown
GUI は以下のタイミングで CLI を subprocess として呼び出す (本仕様は Phase 3/4 の本物化で `tokio::process::Command` で実装される):
```

置換後:

```markdown
GUI は以下のタイミングで CLI を subprocess として呼び出す (`tokio::process::Command` で実装済み):
```

- [ ] **Step 5: `docs/quickstart.md` の ZIP レイアウトを #752 後に直す**

現状:

```text
allaganeye-vX.Y.Z\
├── python\            Python ランタイム（同梱済み、別途インストール不要）
├── lib\               allaganeye 本体
├── ffmpeg\            動画処理エンジン（同梱済み、別途インストール不要）
├── allaganeye.bat     ← このファイルを使います
└── README.txt
```

置換後 (§2.6 の実構造に合わせる):

```text
allaganeye-vX.Y.Z\
├── allaganeye\             allaganeye 本体（Python ランタイム同梱済み、別途インストール不要）
├── ffmpeg\                 動画処理エンジン（同梱済み、別途インストール不要）
├── allaganeye.bat          ← このファイルを使います
├── allaganeye-gui.exe      GUI 本体（allaganeye.bat のダブルクリックで起動します）
├── integrity-manifest.json 同梱物の整合性検査に使います（削除しないでください）
└── README.txt
```

- [ ] **Step 6: 実装と突合する**

Run:

```bash
grep -n "allaganeye.exe\|_internal\|integrity-manifest" scripts/build-portable-zip.ps1 | head -20
grep -n "start_export\|enumerate_h264_encoders\|progress-format" gui/src-tauri/src/lib.rs | head -20
```

Expected: ZIP payload が `allaganeye\allaganeye.exe` + `integrity-manifest.json` を含み、Rust 側に `start_export` / `enumerate_h264_encoders` / `--progress-format` が存在する

- [ ] **Step 7: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 8: Commit**

```bash
git add docs/system-architecture.md docs/quickstart.md
git commit -m "docs(#862): system-architecture §2.1/§2.3 と quickstart の ZIP レイアウトを #752/#761 後の実装に同期 (Refs #862)"
```

---

### Task 18: `docs/versioning.md` / `docs/metadata-spec.md` / `docs/gui-development.md` を直す (P2-33 / #879 / P3)

**Files:**

- Modify: `docs/versioning.md:15` / `:20`
- Modify: `docs/metadata-spec.md` §書き込み方針
- Modify: `docs/gui-development.md:81-88`

- [ ] **Step 1: `docs/versioning.md` の version 管理場所を 3 箇所にする**

現状:

```markdown
`pyproject.toml` の `version` フィールドのみ。
```

置換後:

```markdown
以下の 3 箇所。`/release` のバージョンバンプはこの 3 つをまとめて更新する。

| ファイル | フィールド |
| --- | --- |
| `pyproject.toml` | `version` |
| `gui/src-tauri/tauri.conf.json` | `version` |
| `gui/package.json` | `version` |
```

- [ ] **Step 2: `docs/versioning.md` の旧用語「Director」を直す**

現状:

```markdown
2. Director がレビュー・マージ
```

置換後:

```markdown
2. メンテナ (Idios) がレビュー・マージ
```

- [ ] **Step 3: 3 箇所の version が一致していることを確認する**

Run:

```bash
grep -n '"version"' gui/src-tauri/tauri.conf.json gui/package.json
grep -n '^version' pyproject.toml
```

Expected: 3 つとも同じ値 (現在 `0.3.0`)

- [ ] **Step 4: `docs/metadata-spec.md` §書き込み方針 に #879 の検証層を追記する**

`- **ファイル名**:`metadata.json` 固定 (GUI 編集時のバックアップは `metadata.original.json`)` の**直後**に追加:

```markdown
- **optional field の write 境界検証** (#879): `system_info` / `brightness_samples` / `capture_regions` / `minimap_regions` は present のとき shape を検証してから書き込む。Rust 側 `validate_metadata_for_write` が 4 field の zod schema を mirror し、CLI 側は `brightness_samples` を sanitize する。cache 経由の読み直しは `_load_cache_hit` の単一 read に統一済み (旧 `_load_cache` は廃止)。壊れた optional field を持つ metadata が GUI 経由で書き戻されて後続 run を壊す経路を塞ぐ
```

- [ ] **Step 5: `docs/gui-development.md` の CI ジョブ表を実際の 8 ジョブにする**

現状:

```markdown
[`.github/workflows/ci.yml`](../.github/workflows/ci.yml) に 3 ジョブが定義されている:

| ジョブ | OS | 役割 |
| --- | --- | --- |
| `python` | ubuntu-latest | 既存: ruff / pyright / pytest |
| `gui-frontend` | ubuntu-latest | npm ci → lint → typecheck → build → `gui/dist/` をアーティファクト保存 |
| `gui-rust` | windows-latest | `gui-frontend` のアーティファクトを取得して `cargo check` |
```

置換後:

```markdown
[`.github/workflows/ci.yml`](../.github/workflows/ci.yml) に 8 ジョブが定義されている:

| ジョブ | OS | 役割 |
| --- | --- | --- |
| `python` | ubuntu-latest | ruff / pyright / pytest |
| `hook-test` | ubuntu-latest | `.claude/hooks/` のシェルフック検証 |
| `gui-frontend` | ubuntu-latest | npm ci → lint → typecheck → build → `gui/dist/` をアーティファクト保存 |
| `gui-rust` | windows-latest | `gui-frontend` のアーティファクトを取得して `cargo check` |
| `doc-tauri-commands-drift` | ubuntu-latest | Tauri command 一覧と doc の drift 検出 |
| `doc-error-hint-drift` | ubuntu-latest | エラーヒント文言と doc の drift 検出 |
| `shellcheck` | ubuntu-latest | シェルスクリプトの静的検査 |
| `installer-pester` | windows-latest | `scripts/build-portable-zip.ps1` の Pester テスト |
```

- [ ] **Step 6: ジョブ数を実ファイルと突合する**

Run:

```bash
grep -n "^  [a-z0-9_-]*:" .github/workflows/ci.yml
```

Expected: `jobs:` 直下のジョブが 8 個 (`python` / `hook-test` / `gui-frontend` / `gui-rust` / `doc-tauri-commands-drift` / `doc-error-hint-drift` / `shellcheck` / `installer-pester`)

- [ ] **Step 7: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 8: Commit**

```bash
git add docs/versioning.md docs/metadata-spec.md docs/gui-development.md
git commit -m "docs(#862): versioning の version 3 箇所・旧用語、metadata-spec の #879 検証、gui-development の CI 8 ジョブを反映 (Refs #862)"
```

---

### Task 19: `docs/ui-interaction-spec.md` の anchor 移行を別 issue に起票する

**Files:** (リポジトリ変更なし)

**根拠:** spec §設計判断 4。行番号 anchor 99 箇所を行番号のまま再同期しても、行番号自体が「値の複製」であり CI ガードが無い以上 GUI が 1 PR 動くだけで再 drift する。根治には anchor 形式を名前参照へ移行する設計判断が必要で、これは再同期ではなく形式移行。Iron Law 3 により別 issue。

- [ ] **Step 1: 現状の stale 数を数える**

Run:

```bash
grep -c "\.tsx:[0-9]" docs/ui-interaction-spec.md
```

Expected: anchor の総数が出る (spec 記載時点で 99)

- [ ] **Step 2: issue を起票する**

`printf` + `gh issue create --body-file -` で日本語本文を渡す (inline 引数は UTF-8 破損)。本文に含める要素:

- 背景: `docs/ui-interaction-spec.md` の `[File.tsx:NNN]` 形式 anchor が GUI コード増分で stale 化
- 問題: 行番号は「値の複製」そのもの。`docs/coding-conventions.md` の SSoT 規約が潰そうとしている構造的再発要因を再生産する
- 提案: anchor 形式を名前参照 (`ExportScreen.tsx » handleExportClick` 等) へ移行
- 受け入れ条件: 全 anchor が名前参照になっている / 参照先シンボルが実在することを検証する手段がある
- 出自: `Refs #862` (W6 doc 一括再同期でスコープ外と判断)

- [ ] **Step 3: #862 に起票結果をコメントする**

起票した issue 番号を `#862` にコメントし、spec §設計判断 4 の「別 issue に切り出す」が履行されたことを記録する。

---

### Task 20: PR-B の全体ゲートと PR 作成

**Files:** (変更なし。検証と PR 作成のみ)

- [ ] **Step 1: doc のみの変更であることを確認する**

Run:

```bash
git diff --name-only origin/develop-0.3.0
```

Expected: `.md` ファイルのみ。Python / Rust / TS は含まれない

- [ ] **Step 2: markdownlint**

Run: `bash scripts/check-markdownlint.sh`
Expected: `Summary: 0 error(s)`

- [ ] **Step 3: doc drift gate の対象ファイルを触っていないことを確認する**

Run:

```bash
grep -n "paths:" -A 8 .github/workflows/ci.yml | grep -A 8 "doc-tauri-commands-drift\|doc-error-hint-drift"
```

Expected: gate が参照する doc / source を本 PR が変更していない。変更していた場合は該当 job をローカルで再現してから進む

- [ ] **Step 4: PR Pre-flight (Iron Law 6)**

Run:

```bash
gh pr list --search "862" --state open
git fetch origin develop-0.3.0
git log HEAD..origin/develop-0.3.0 --oneline
gh pr list --search "862" --state all
```

Expected: PR-A が merge 済みで base に取り込まれている。他の並行 PR なし

- [ ] **Step 5: Codex adversarial-review (Pre-flight Step 5)**

focus に `doc claims verified against source not assumed / scorebar center-straddling vs longest run direction / measurement records preserved / CLAUDE.md module table completeness` を渡す (ASCII)。

- [ ] **Step 6: PR を作成する**

`develop-0.3.0` を base に PR-B を作成する。`printf | gh pr create --body-file -`。`Refs #862` のみ。

- [ ] **Step 7: merge を Idios に依頼する**

`AskUserQuestion` で PR-B の merge を依頼する。

- [ ] **Step 8: 受け入れ条件を逐条検証して #862 をクローズする**

両 PR merge 後、spec §受け入れ条件 の 9 項目を逐条引用し、対応する diff を逐条引用してから `gh issue close` する (Iron Law 1 / 4)。

---

## 検証まとめ

| 対象 | チェック | 実行タイミング |
| --- | --- | --- |
| 両 PR | `bash scripts/check-markdownlint.sh` | 各 commit 前 + PR 作成前 |
| PR-A | `python -m ruff check .` / `python -m ruff format --check .` / `python -m pyright` / `pytest` (全 repo、引数なし) | Task 11 |
| PR-B | doc のみ。markdownlint のみ | Task 20 |

ロジック変更ゼロのため実機検証 trigger (GPU / audio / 長時間動画 / GUI Tauri) は非該当。
