# Audit Remediation Wave 1 — N2 (#817) skill 改修 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development for mechanical parts; skill content changes follow **empirical-prompt-tuning** (CLAUDE.md §skill 改修ワークフロー). Steps use checkbox (`- [ ]`).

**Goal:** 「closed 側から open 側への参照切れ」3 類型を skill 改修で構造的に防ぐ — (1) close-issue にクローズ前の follow-up 宣言起票漏れチェックを追加、(2) release の deferred レビューに本文鮮度 + not_planned 残タスク検出を追加 + version bump grep に `*.json` 追加、(3) skills の broken link 14 件を修正。

**Architecture:** 3 part に分離。Part A (broken link 14 件) と Part B-version-grep は機械的修正 (eval 不要、memory「link 修正は skip 可」)。Part B-content (close-issue / release のプロンプト挙動変更) は empirical-prompt-tuning protocol (fresh subagent eval + 2 consecutive clears) を要する。各 skill の `eval/requirements.md` に新規 [critical] 要件を追加してから loop を回す。

**Tech Stack:** Markdown (SKILL.md) / markdownlint / Agent tool (eval subagent, sonnet, run_in_background) / gh api (mizchi protocol 参照)

**元 issue:** #817 (P2-medium, task, l2-workflow)。AC は確立済み運用どおり **spec §N2 設計 + issue 確認項目 4 件** を逐条検証:

1. close-issue: 「別 issue で追跡」宣言の行き先確認 step を追加
2. release: deferred レビューに本文鮮度 + not_planned 残タスク確認を追加、bump grep に `*.json` を追加
3. skills の `../../docs/` → `../../../docs/` 一括修正 (14 件)
4. empirical-prompt-tuning protocol で検証し Self-Test Report に記録

**前提条件 / 事実 (2026-06-13 実測):**

- main worktree = `develop-0.3.0` @ 8a0ebc8 (clean)。session-id: `audit-w1-n2-20260613`、branch: `claude/audit-w1-n2`
- **broken link 14 件の正確な所在** (anchored grep `\]\(\.\./\.\./docs/`): scope-guard (1: L63) / review-pr (8: L55,166,204,231,254,327,328,329) / create-task (1: L89) / iterate-review (4: L24,61,63,174)。release・close-issue は broken ゼロ
- 注意: 素の `../../docs/` grep は `../../../docs/` (正) の部分文字列にも一致するため count=21 と過大に出る。**必ず `](` アンカー付き (`](../../docs/`) で計測**。修正は `](../../docs/` → `](../../../docs/` の literal 置換で、正リンク `](../../../docs/` には触れない (部分文字列衝突なしを確認済み)
- `.claude/skills/<name>/SKILL.md` は repo root から深さ 3 → `docs/` への正しい相対は `../../../docs/`
- close-issue eval: `requirements.md` + scenario A/B/C + reports 一式あり (#594)。Step 5b に既存「参照先 PR/issue の実在確認」あり (ただし**番号が言及されている場合**のみ発火。番号なし follow-up 宣言は死角 = P2-39)
- release eval: `requirements.md` のみ (scenario A/B/C は requirements.md 内に定義、別ファイルなし)
- release version bump: 現 Step 3-2 (L109) は `grep -r '<旧バージョン>' --include='*.py' --include='*.toml'`。`*.json` 不在 (P2-33: tauri.conf.json / package.json を拾わない)
- empirical-prompt-tuning protocol: docs/l2-workflow.md §795 + memory `feedback_skill_empirical_tuning`。mizchi SsoT を `gh api repos/mizchi/skills/contents/meta/empirical-prompt-tuning/SKILL.md -H "Accept: application/vnd.github.raw"` で都度参照 (vendoring/commit 禁止)。fresh subagent (sonnet, 並列, run_in_background)、Iteration 1 再評価は必ず新規 subagent、2 consecutive clears で打ち切り
- 検証: markdownlint + empirical-prompt-tuning。コード変更なし → Iron Law 6 実機検証 trigger 非該当

---

## File Structure

| 区分 | path | 責務 |
| --- | --- | --- |
| Create | `docs/superpowers/plans/2026-06-13-audit-w1-n2.md` | 本 plan |
| Modify | `.claude/skills/scope-guard/SKILL.md` | broken link 1 |
| Modify | `.claude/skills/review-pr/SKILL.md` | broken link 8 |
| Modify | `.claude/skills/create-task/SKILL.md` | broken link 1 |
| Modify | `.claude/skills/iterate-review/SKILL.md` | broken link 4 |
| Modify | `.claude/skills/close-issue/SKILL.md` | Step 5b に follow-up 宣言起票漏れチェック追加 |
| Modify | `.claude/skills/close-issue/eval/requirements.md` | 新規 [critical] 要件 (follow-up 検出) |
| Modify | `.claude/skills/release/SKILL.md` | deferred レビュー追加 (Step 0c 拡張) + version grep に `*.json` |
| Modify | `.claude/skills/release/eval/requirements.md` | 新規 [critical] 要件 (鮮度 / not_planned / json grep) |

---

## Part A: broken link 一括修正 (機械的、eval 不要)

### Task A1: branch + plan commit (controller)

- [ ] **Step 1:**

```bash
git branch --show-current   # develop-0.3.0
git status --short          # untracked が本 plan のみ
git fetch origin develop-0.3.0
git checkout -b claude/audit-w1-n2 origin/develop-0.3.0
git add docs/superpowers/plans/2026-06-13-audit-w1-n2.md
git commit -F - <<'EOF'
doc: N2 skill 改修の実装 plan を追加 (Refs #817)

audit remediation Wave 1 N2。spec §N2 の just-in-time plan
(broken link 14 件の anchored 実測 + close-issue/release 改修の
empirical-prompt-tuning 手順)。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
```

### Task A2: broken link 14 件を置換 (1 commit)

**Files:** scope-guard / review-pr / create-task / iterate-review の SKILL.md

- [ ] **Step 1:** 各ファイルで `](../../docs/` を `](../../../docs/` に置換 (Edit の replace_all は同一文字列のみなので、Edit ツールでファイルごとに `replace_all: true` で `](../../docs/` → `](../../../docs/` を実行)。対象は上記 4 ファイルのみ (release/create-task の正リンク `](../../../docs/` は文字列が異なるため無変更)

- [ ] **Step 2:** 置換後の検証

```bash
# broken が 0 になったこと (anchored)
[ "$(grep -rl '](\.\./\.\./docs/' .claude/skills | wc -l)" = "0" ] && echo "BROKEN_ZERO"
# 正リンクが増えていること
grep -rc '](\.\./\.\./\.\./docs/' .claude/skills
```

Expected: `BROKEN_ZERO`。

- [ ] **Step 3:** 置換後リンクの実在確認 (サンプル)

```bash
ls docs/l2-workflow.md docs/refactor-pattern.md docs/markdownlint-guide.md docs/superpowers/specs/2026-05-10-iterate-review-and-review-pr-redesign.md
```

- [ ] **Step 4:** markdownlint

```bash
bash scripts/check-markdownlint.sh 2>&1 | grep -E "scope-guard|review-pr|create-task|iterate-review" || echo LINT_OK_LINKS
```

- [ ] **Step 5:** commit

```bash
git add .claude/skills/scope-guard/SKILL.md .claude/skills/review-pr/SKILL.md .claude/skills/create-task/SKILL.md .claude/skills/iterate-review/SKILL.md
git commit -F - <<'EOF'
fix(skills): broken な ../../docs/ リンク 14 件を ../../../docs/ に修正 (Refs #817)

.claude/skills/<name>/SKILL.md は repo root から深さ 3 のため docs/ への
正しい相対は ../../../docs/。../../docs/ (1 階層不足、.claude/ 止まり) の
14 件 (scope-guard 1 / review-pr 8 / create-task 1 / iterate-review 4) を
修正。](アンカーで正リンクに非衝突を確認済 (audit P3)。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
```

## Part B-1: release version bump grep に *.json 追加 (機械的)

### Task B1: release SKILL.md Step 3-2 の grep 拡張

- [ ] **Step 1:** `.claude/skills/release/SKILL.md` L109 の
  `grep -r '<旧バージョン>' --include='*.py' --include='*.toml'` を
  `grep -r '<旧バージョン>' --include='*.py' --include='*.toml' --include='*.json'` に変更し、直後に「`*.json` は `gui/src-tauri/tauri.conf.json` / `gui/package.json` 等のバージョン参照を拾う (#817/P2-33)」と注記。
  ※ この commit は Part B-2 の release content 変更と同一 commit にまとめる (Task C3 で一括 commit)。本 Task では編集のみ。

## Part B-2: close-issue / release コンテンツ変更 (empirical-prompt-tuning)

> 以降は controller (主セッション) が empirical loop を orchestrate する。mizchi protocol を `gh api repos/mizchi/skills/contents/meta/empirical-prompt-tuning/SKILL.md -H "Accept: application/vnd.github.raw"` で取得して都度参照 (commit しない)。

### Task C1: close-issue SKILL.md 改修 + eval 要件追加

- [ ] **Step 1: SKILL.md 編集**: `### 5b. 受け入れ条件以外のチェックボックス確認` の既存「参照先 PR/issue の実在確認」ブロックの後 (「PR 本文に書かれているから対応済み...」の行の前) に以下を挿入:

```markdown
**follow-up 宣言の起票漏れ確認 (番号なし、#817 / audit P2-39)**: クローズ対象 PR の**本文およびレビューコメント** (`gh pr view <PR#> --comments`) に「別 issue で追跡 (予定)」「follow-up で対応」「(guard 等) 別リポジトリ側で対応」のような**将来対応の宣言があるのに行き先 issue 番号が書かれていない**場合、その follow-up は未起票の死角になっている可能性が高い (P2-39: PR #811 の guard repo follow-up が未起票だった事例)。番号なし宣言を「未追跡の残タスク」として Step 6 で (B) `/create-task` 起票を提案する (番号が書かれている場合は上記『参照先 PR/issue の実在確認』に従う)。「宣言はあるが番号がないので追えない」と見送るのは握り潰しパターン。
```

- [ ] **Step 2: requirements.md に新規 [critical]**: `close-issue/eval/requirements.md` のシナリオ A (verbose mode、PR #921) に項目 11 を追加 (番号なし follow-up 宣言の検出)。例: 「11. **[critical]** クローズ対象 PR #921 の本文/レビューコメントに番号なしの follow-up 宣言 (例「guard 側で追って対応」) がある場合、それを未追跡残タスクとして Step 6 (B) 起票提案している (番号がないことを理由に見送っていない)」。シナリオ本文 (mock #921) にも「レビューコメントに番号なし follow-up 宣言を 1 つ含む」と前提追記。

### Task C2: release SKILL.md 改修 + eval 要件追加

- [ ] **Step 1: SKILL.md 編集 (deferred レビュー追加)**: `### Step 0c` の後に新サブ節 `#### Step 0c-2: deferred 本文鮮度 + not_planned 残タスク確認 (#817 / audit P2-39)` を追加:

```markdown
#### Step 0c-2: deferred 本文鮮度 + not_planned 残タスク確認 (#817 / audit P2-39)

(a)/(b)/(c) 分類の前に、以下 2 点を確認する:

- **本文鮮度**: 各 deferred issue の本文と直近コメントを、関連 spec / design doc と突合する。本文が現状の実装・方針と矛盾 (鮮度切れ) している場合は、分類前に `gh issue edit` での本文更新を提案する (古い前提のまま (b) 継続すると次 cycle も腐る)
- **not_planned 残タスク**: リリース区間 (前タグ〜HEAD、`git log <前タグ>..HEAD`) で参照される issue 番号のうち、コード/doc 内マーカー (`wired in #N` / `Refs #N` / `TODO(#N)` 等) が指す #N が **not_planned で close** されている場合 (`gh issue view #N --json state,stateReason` で `stateReason == "not_planned"`)、その残タスクの行き先 (再起票要否) を確認する (#762 not_planned close で残タスクが orphan 化した事例)
```

- [ ] **Step 2: version grep (Task B1 の編集をここで取り込み)**: Part B-1 の `*.json` 追加が未適用なら適用。

- [ ] **Step 3: requirements.md に新規 [critical]**: `release/eval/requirements.md` のシナリオ B (patch, deferred 5 件) に項目を追加: 「**B-7 [critical]**: Step 0c-2 で各 deferred issue の本文鮮度を spec と突合し、矛盾があれば本文更新を提案している」「**B-8 [critical]**: リリース区間の `wired in #N` 等が指す not_planned close issue を検出し残タスク行き先を確認している」。シナリオ A (minor) に「**A-5 [critical]**: version bump で `grep --include='*.json'` を含め tauri.conf.json / package.json のバージョン参照も確認している」。

### Task C3: empirical-prompt-tuning loop (2 consecutive clears)

> close-issue と release を**別々に** loop する (eval scenario が独立)。各 skill で以下を回す。

- [ ] **Step 1: mizchi protocol 取得**: `gh api repos/mizchi/skills/contents/meta/empirical-prompt-tuning/SKILL.md -H "Accept: application/vnd.github.raw"` (作業 dir に短期キャッシュ可、commit 禁止)。§ワークフロー 4 両面評価の精度算出・[critical] 規則に従う

- [ ] **Step 2: Iteration 0 (baseline、編集前 SKILL.md で新 [critical] の gap を実証)**: 各シナリオにつき fresh subagent (`general-purpose`, `model: sonnet`, `run_in_background: true`) を dispatch。prompt: 「`/<skill> <番号>` を呼ばれた状況を想定し、**現在の SKILL.md の手順を辿って** どこで stop したか・どの判断をしたか・最終出力を報告。self-reread でなく手順シミュレーション」。新 [critical] 項目が baseline で × (= 未実装の gap) を確認

- [ ] **Step 3: SKILL.md 編集**: Task C1/C2 の編集を適用 (まだなら)

- [ ] **Step 4: Iteration 1 (新規 subagent で再評価)**: Iteration 0 とは**別の** fresh subagent を dispatch (同一 agent 使い回し禁止)。全 [critical] (既存 + 新規) が ○、既存シナリオに regression なしを確認。落ちた項目は構造化 reflection (Issue / Cause / General Fix Rule) → SKILL.md 修正

- [ ] **Step 5: Iteration 2 (新規 subagent)**: clear 条件 (new unclear=0 / accuracy +3pt 以下 / step ±10% / duration ±15%) を確認。Iteration 1 と 2 が**連続 clear**なら打ち切り。未収束なら reflection → 修正 → 追加 Iteration (構造的欠陥が解消されていれば打ち切り可、残細部は deferred issue)

- [ ] **Step 6: eval reports 保存**: `close-issue/eval/reports/` / `release/eval/reports/` に iter_N_*.md として記録 (既存 close-issue reports の形式に倣う)

- [ ] **Step 7: commit (close-issue / release を別 commit)**:

```bash
# close-issue
git add .claude/skills/close-issue/SKILL.md .claude/skills/close-issue/eval/
git commit -F - <<'EOF'
feat(skills): close-issue に番号なし follow-up 宣言の起票漏れチェックを追加 (Refs #817)

クローズ対象 PR の本文/レビューコメントに行き先番号なしの follow-up 宣言が
あれば未追跡残タスクとして起票提案する step を Step 5b に追加 (audit P2-39:
PR #811 guard follow-up 未起票の死角)。empirical-prompt-tuning で検証
(新規 [critical] + 既存シナリオ regression なし、Self-Test Report 参照)。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
# release
git add .claude/skills/release/SKILL.md .claude/skills/release/eval/
git commit -F - <<'EOF'
feat(skills): release deferred レビューに鮮度/not_planned 確認 + json grep を追加 (Refs #817)

Step 0c-2 (deferred 本文鮮度突合 + not_planned close 残タスク検出) を追加し、
version bump grep に --include='*.json' を追加 (tauri.conf.json/package.json)。
audit P2-33/P2-39。empirical-prompt-tuning で検証 (Self-Test Report 参照)。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
```

## Task D: Pre-flight + PR (controller)

- [ ] Step 0: `gh pr list --search "817" --state open` → 0 件
- [ ] Step 1-3: `git fetch origin develop-0.3.0` → `git log HEAD..origin/develop-0.3.0 --oneline` → 0 行
- [ ] Step 4: `gh pr list --search "817" --state all` → 重複なし
- [ ] Step 5: codex adversarial-review (companion script、`--background`、focus ASCII: skill prompt edits don't regress existing eval scenarios / broken-link replacement安全性 / Iron Law 3 scope / follow-up detection false-positive)。不可なら C6 fallback + PR notice
- [ ] Step 6: push + PR (base develop-0.3.0, Refs #817)。**Self-Test Report に empirical-prompt-tuning section** (Iteration table: iteration / changes / per-scenario success・accuracy・steps・duration / structured reflection / ledger) を close-issue・release 各々記載 (memory `feedback_skill_empirical_tuning`)。markdownlint `[x]`、実機検証「該当なし (コード変更なし)」

## Task E: /iterate-review + close (controller)

- [ ] `/iterate-review <PR#>` 自走。収束後 Idios merge → `/close-issue 817`

## マージ後 (記録のみ)

- `/close-issue 817` (AC 4 件逐条 + empirical-prompt-tuning 記録の実在確認)
